# SK Hynix 위험 Unit 사전 탐지 시스템 — 대시보드 & AI Agent 실행 계획

**주제**: Wafer Test 기반 위험 Unit 사전 탐지 시스템
**부제**: 품질 예측 대시보드 구축 및 AI를 활용한 의사결정 지원

> 작성일: 2026-04-23 | 갱신: 2026-05-21
> 최종 발표: 2026-06-09

---

## 0. 프로젝트 목적 & 사용자

### 배경
반도체 생산에서 Field 불량은 **WT 완료 후 수개월이 지나야** 확인된다.

```
WT 완료 → PKG(15일) → PKT(15일) → 현장 투입 → health 수집(1~3개월 소요)
```

이 시간 동안 불량 위험 unit은 이미 출하된 상태이며, 문제를 인지했을 때는
손쓸 수 없는 상황이 된다. **WT 측정값(X0~X1086)으로 미리 위험을 탐지**하는 것이 이 시스템의 존재 이유다.

---

### 목적

> **WT 단계에서 Field 불량 위험이 높은 unit을 사전에 식별하고,
> PI가 즉시 의사결정할 수 있도록 시각화 + AI 분석을 제공한다.**

구체적으로 세 가지를 답한다:

| 질문 | 답 |
|------|-----|
| **지금 위험한 unit이 뭔가?** | Val/Test 예측 결과 기반 위험 unit 목록 |
| **왜 위험한가?** | 주요 불량 유발 Feature 분석 |
| **어디서 났는가?** | Lot / Wafer / Position / 웨이퍼맵 |

---

### 데이터 시나리오 (핵심)

이 시스템은 **두 가지 데이터 상태**를 동시에 다룬다.

```
Train  →  WT 값 + 실제 health(Y) 모두 있음
           "과거 생산분 — 불량 패턴이 이미 확인된 데이터"
           역할: 모델 학습 + 패턴 파악

Val    →  WT 값만 있음, health(Y)는 아직 없음
           "방금 생산된 제품 — 아직 Field 결과가 안 나온 상태"
           역할: 실제 예측 대상 (PI가 지금 봐야 하는 것)

Test   →  Val과 동일한 상태 (최종 제출용)
```

> **대시보드의 메인은 Val이다.**
> "지금 이 unit들이 위험한가?"에 답하는 것이 핵심.
> Train은 "왜 이 패턴이 위험한지" 설명하는 보조 역할.

---

### 표현 원칙

모델 성능 지표(RMSE, AUC, clf_proba 수치)는 전면에 노출하지 않는다.
PI가 알아야 하는 것은 **"이 unit이 위험한가, 얼마나, 왜"**이지 모델 평가 지표가 아니다.

| 내부 표현 | 화면 표현 |
|----------|----------|
| Grade 4 (reg_pred ≥ g3, p99.95 이상) | 🔴 위험 (Critical) |
| Grade 3 (g2 ≤ reg_pred < g3, p70.8~p99.95) | 🟠 고위험 |
| Grade 2 (g1 ≤ reg_pred < g2, p50~p70.8) | 🟡 관찰 |
| Grade 1 (reg_pred < g1, p50 미만) | 🟢 안전 |
| reg_pred 수치 × 1,000,000 | 예측 불량 ppm |
| health > 0 (Train) | 실제 불량 확인 |
| RMSE, AUC 등 | 모델 분석 탭에서만 (별도 분리) |

> **위험 등급 기준 (현재 확정: train 전체 reg_pred 분위수 기반, 고정값)**
> - **g1 = p50** (Grade 1/2 경계) — train 전체의 중앙값
> - **g2 = p70.8** (Grade 2/3 경계) — train 실제 불량 비율 29.2%와 일치하는 경계
> - **g3 = p99.95** (Grade 3/4 경계) — 임계값, 이 이상 = Critical
> - 대시보드 로드 시 train split 기준으로 동적 계산 (`computeThresholds()` in Overview.jsx)
> - **과거 검토 방법**: ① 50%/70.8%/99.95% 분위수 (현재 채택), ② IQR 기반(Q2/Q3/Q3+1.5×IQR) 검토 중
>
> **최종 예측 모델**: 15개 base 모델 스태킹 앙상블
> - Feature Importance, SHAP 분석은 **ZITboost 계열 모델(zit_only)** 기준으로만 산출
> - clf_proba: ZITboost 내부 π(x) — 참고용으로만 유지, 위험 분류 기준은 reg_pred 사용

---

### 사용자
- **1차**: PI (Process Integration 엔지니어) — 수율 점검, 불량 원인 파악, 공정 개선
- **2차**: 포트폴리오 열람자 — "현업 수준의 ML 시스템을 설계·구현했다"는 것을 보여주는 결과물

### PI 의사결정 흐름
```
① 위험 현황 파악   →  Overview (지금 위험한 unit 몇 개?)
② 위치 파악        →  웨이퍼맵 / 위치 분석 (어느 Lot, 어느 위치?)
③ 원인 파악        →  변수 분석 (왜 위험한가?)
④ 조치 결정        →  AI Agent (어떻게 해야 하나?)
⑤ 보고서 작성      →  AI Agent (보고서 자동 생성)
```

---

## 1. 대시보드 vs Agent 역할 분리

### 핵심 원칙
```
대시보드 = "무슨 일이 일어나고 있나"  →  숫자와 차트로 현상 시각화
Agent    = "그래서 어떻게 해야 하나"  →  원인 분석 + 조치 제안 + 보고서 생성
```

### 대시보드가 답하는 것 (정적 시각화)
| PI 질문 | 대응 화면 |
|---|---|
| 지금 위험한 unit이 몇 개야? | Overview — 불량률 / 위험집중 Lot / HIGH 평균 불량지수 KPI 카드 |
| 어느 lot에서 많이 났어? | 위치 분석 — Lot 바차트 (Lot 클릭 → Wafer 바차트 → 웨이퍼맵 드릴다운) |
| 가장 위험한 unit이 어디야? | 위험 Unit 목록 — Top N 테이블 |
| 왜 위험하다고 판단했어? | 변수 분석 — Feature Importance |
| 불량/정상 그룹 차이가 뭐야? | 그룹 비교 |
| 특정 unit 상세가 궁금해 | 데이터 테이블 |

### Agent가 답하는 것 (동적 추론)
| PI 질문 | Agent 기능 |
|---|---|
| 왜 이번 lot A에서 특히 많이 났어? | 원인 분석 |
| 이 패턴 이전에도 있었어? | 패턴 비교 |
| X005가 높으면 어떻게 조치해야 해? | 조치 제안 |
| 개선조치 보고서 써줘 | 보고서 자동 생성 |
| 앞으로 어떻게 막아? | 방어선 제안 |

---

## 2. 전체 레이아웃

```
┌─────────────────────────────────────────────────────────┐
│  TopBar: 로고 / 날짜 / 알림 🔔 / 프로필                  │
├─────────────┬───────────────────────────────────────────┤
│             │                                           │
│  좌측       │  메인 콘텐츠 영역                          │
│  사이드바   │  (선택 메뉴에 따라 변경)                   │
│             │                                           │
│  📊 Overview│                                           │
│  🗺 웨이퍼맵│                                           │
│  📍 위치분석│                          [🤖 챗봇 버튼]   │
│  🔬 변수분석│                                           │
│  📈 그룹비교│                                           │
│  📋 데이터  │                                           │
└─────────────┴───────────────────────────────────────────┘
```

---

## 3. 메인페이지 (Overview) 구성

**"지금 얼마나 위험한가"를 한눈에 파악하는 페이지 — 최신 lot(어제 하루치) 기준**

```
┌─────────────────────────────────────────────────────────┐
│  📅 기준일: YYYY-MM-DD (Lot N) — 어제 WT 완료분           │
│  KPI 카드 3개  (어제 WT 완료 lot 하루치 기준)              │
│  [📊 불량률 N%      ]  [🏭 위험집중 Wafer]  [🔴 HIGH 평균 불량지수]  │
│  [위험 N개 / 전체 N개]  [Wafer XX      ]  [reg_pred 평균값      ]  │
├─────────────────────────────────────────────────────────┤
│  수율 트렌드 (날짜 x축, train+val만, test 제외)            │
│  ━━━ 예측 수율%(라인, val)  ▓▓▓ 실제 수율%(영역, train)   │
│  |←── 03-27~05-11(train) ──→|←── 05-12~06-10(val) ──→| │
│                              ↑ 구분선   가장 오른쪽=어제  │
│  [드래그 슬라이더로 날짜 범위 조절 가능]                   │
├──────────────────────────┬──────────────────────────────┤
│  Wafer별 위험 unit 수     │  위험 등급별 unit 수          │
│  (어제 하루치 기준)        │  (어제 하루치 기준)           │
└──────────────────────────┴──────────────────────────────┘
```

### 메인페이지에 넣는 것
- KPI 카드 3개 (**최신 lot — 어제 하루치 기준**, 기준일 텍스트 상단 표시):
  - **불량 위험률** — 최신 lot unit 중 `reg_pred >= 불량 threshold` 비율(%). 아래 작게 "위험 N개 / 전체 N개"
  - **위험 집중 Wafer** — 최신 lot 안에서 위험 unit이 가장 많은 wafer 번호
  - **HIGH 그룹 평균 불량지수** — 어제 lot 안에서 y_pred 상위 10% unit들의 평균값
- **수율 트렌드 차트**: 날짜(YYYY-MM-DD) x축, train+val만(test 제외), 가장 오른쪽=어제. 예측 수율(라인) + 실제 수율(영역, train만). 하단 드래그 슬라이더로 날짜 범위 조절 가능.
- **Wafer별 위험 unit 수 바차트** — 어제 하루치 기준
- **위험 등급별 unit 수 바차트** — 어제 하루치 기준 (HIGH/MED/LOW)

### 메인페이지에 넣지 않는 것
- Lot별 위험 집중도 바차트 → 웨이퍼맵 페이지로 이동
- Position별 위험도 바차트 → 위치 분석 페이지로 이동
- Train/Val/Test 탭 — 트렌드 차트 안에서 구분선으로 처리
- 위험 Unit Top 10 테이블 — 모델 신뢰도 한계로 제거
- RMSE, AUC, clf_proba 수치 → 모델 성능 탭으로만
- 피처 분석, 그룹 비교 → 사이드바 세부 페이지

---

## 4. 사이드바 페이지 구성

현재 구현 상태와 데이터 연결 여부를 함께 표기한다.

| 메뉴 | 페이지 | 데이터 | 상태 |
|------|--------|--------|------|
| 📊 Overview | 위험 현황 요약 | dashboard_units.csv, trend_data.csv, shap_beeswarm.csv | ✅ 구현 완료 |
| 🗺 웨이퍼맵 | Lot→Wafer→die 히트맵 드릴다운 | wafer_map.csv | ✅ 구현 완료 |
| 🔬 계층별 정밀 분석 | Lot→Wafer→Unit 드릴다운 | wafer_map.csv, dashboard_units.csv | ✅ 구현 완료 |
| 📈 모델 분석 | Feature Importance + SHAP + 분포 + 산점도 | feature_importance.csv, shap_bar.csv, feature_dist.csv, dashboard_units.csv, metrics.csv | ✅ 구현 완료 |

> **사이드바 메뉴 (현재 확정)**
> - 개요 (Overview)
> - 계층별 정밀 분석 (Drilldown)
> - 모델 분석 (ModelPerformance)
>
> **제거된 계획 페이지** (미구현 → 현재 구현 범위 외): 위치 분석(Position/Lot/Wafer 탭), 그룹 비교, 데이터 테이블 별도 탭

---

### 📊 Overview (메인)
**"지금 얼마나 위험한가"** — ✅ 구현 완료 (2026-05-21 기준)

- **KPI 카드 3개** (전체 데이터 기준, "이번주 WW37" 컨셉):
  - **이번주 검사 unit** — 전체 unit 수
  - **평균 예측 ppm** — 전체 reg_pred × 1,000,000 평균
  - **위험 unit** — Grade 4 (p99.95 이상) 개수, 아래 sub: "상위 0.05% · p99.95 이상"
- **주차별 불량 ppm 트렌드 차트** (trend_data.csv):
  - x축 = WW 주차, y축 이중(생산량 막대 + 불량 ppm 라인)
  - 실측 구간(회색) / 예측 구간(파랑) / 최신 주차(빨강 점선) 3분할
- **위험 유닛 테이블** (Grade 4, 행 클릭 → 계층별 정밀분석 이동):
  - 컬럼: Lot / Wafer / Unit(ufs_serial) / 예측 ppm
- **위험 유닛 SHAP Top10** (shap_beeswarm.csv, Grade 4 기준):
  - 빨강 = 불량 증가 방향, 파랑 = 불량 감소 방향

---

### 🗺 웨이퍼맵
**"어느 위치에서 불량이 났는가"** — ✅ 구현 완료

- **날짜 선택 → Lot 선택**: 달력 방식으로 날짜 클릭 후 해당 날짜의 lot 선택
- **Wafer 선택**: 선택 Lot의 wafer 목록 (위험 unit 수 순)
- **웨이퍼맵 SVG**: die_x/die_y 실좌표 기반 산점도, 색상 = 예측값 (정상=파랑계열, 위험=노랑→빨강)
- **threshold**: p70.8 기준 (전체 데이터 preds 정렬 기준 동적 계산)
- 데이터: wafer_map.csv (die 단위, pred 컬럼)

---

### 🔬 계층별 정밀 분석 (Drilldown)
**"특정 Lot/Wafer/Unit을 파고들어 원인 파악"** — ✅ 구현 완료

- **좌측 Lot 트리**: Lot 목록 (위험률 정렬) → 클릭 시 해당 Lot의 Wafer 목록 펼침
- **중앙 WaferMap SVG**: 선택된 Wafer의 die 히트맵 (색상 = pred 크기)
- **우측 Unit 진단**: 선택된 Unit의 verdict(위험/정상) + 예측 ppm + 등급 표시
- **탭**: Lot 탭 / Wafer 탭 / Unit 탭 / Die 탭
- Overview Grade 4 테이블에서 행 클릭 시 해당 Unit으로 직접 이동 가능
- 데이터: wafer_map.csv + dashboard_units.csv

---

### 📈 모델 분석
**"모델이 왜 이렇게 판단했는가"** — ✅ 구현 완료

**KPI 행**:
- 앙상블 모델 검증 오차(RMSE) — stacking 또는 ensemble 모델 val RMSE
- 기준 대비 개선 — 베이스라인 RMSE 0.015 대비 개선율(%)
- 중요 피처 1~5위 — feature_importance.csv 기준

**2×2 차트 그리드**:
- **좌상 — 피처 중요도 Top20**: feature_importance.csv의 lgbm_gain 기준, 막대 클릭 → 우하/좌하 연동
- **우상 — SHAP 기여도 Top20**: shap_bar.csv (ZITboost 계열 zit_only 기준), 빨강/파랑 방향 표시
- **좌하 — 피처 분포 비교**: Grade1(고위험) vs Grade4(저위험) 밀도 라인, feature_dist.csv
- **우하 — 피처 산점도**: 선택 feature vs health 산점도, Grade1/4 색상 구분

> **중요**: Feature Importance와 SHAP 분석은 모두 **ZITboost 계열 모델(zit_only) 기준**으로만 산출.
> 15개 base 모델 스태킹의 전체 ensemble이 아닌 ZIT 모델만의 해석력을 활용.

---

## 5. 챗봇 (AI Agent) 구성

### 열기 방식
- 우측 하단 고정 버튼 (🤖)
- 클릭 시 오른쪽에서 슬라이드로 패널 열림
- 메인 콘텐츠 영역이 좁아지는 push 방식
- 닫으면 원래대로 복귀

### 탭 구조
```
┌─────────────────────────────┐
│  [  Assistant  ] [  Agent  ] │  ← 탭 클릭으로 전환
├─────────────────────────────┤
│                             │
│  Assistant 탭:              │
│  (더미 — 추후 구현)          │
│                             │
│  Agent 탭:                  │
│  (더미 — 추후 구현)          │
│                             │
├─────────────────────────────┤
│  [입력창          ] [전송]   │
└─────────────────────────────┘
```

### Assistant 탭
- 단순 질문 답변 (더미)
- 예: "불량률이 뭐야?", "SHAP이 뭐야?"

### Agent 탭
- 데이터 기반 분석 + 추론 + 보고서 생성 (더미)
- 예: "이번 lot 왜 불량 많아?", "보고서 써줘"

---

## 6. 컴포넌트 구조

```
src/
├── main.jsx
├── App.jsx                        # 라우팅
├── styles/
│   └── variables.css              # 컬러 변수, 폰트
├── components/
│   ├── layout/
│   │   ├── TopBar.jsx             # 상단 바 (로고, 날짜, 알림, 프로필)
│   │   ├── Sidebar.jsx            # 좌측 네비게이션
│   │   └── Layout.jsx             # 전체 레이아웃 래퍼
│   ├── common/
│   │   ├── KpiCard.jsx            # KPI 숫자 카드
│   │   ├── ChartCard.jsx          # 차트 컨테이너 카드
│   │   └── RiskBadge.jsx          # HIGH / MED / LOW 뱃지
│   ├── charts/
│   │   ├── HeatmapChart.jsx       # 웨이퍼맵 히트맵
│   │   ├── MiniHeatmap.jsx        # Overview용 미니 웨이퍼맵
│   │   ├── TrendChart.jsx         # 불량률 트렌드
│   │   ├── BarChart.jsx           # 위치별 바차트
│   │   ├── ShapChart.jsx          # SHAP 영향도
│   │   ├── DistChart.jsx          # 불량/정상 분포 비교
│   │   └── GroupCompareChart.jsx  # 그룹 비교 바차트
│   └── chatbot/
│       ├── ChatBot.jsx            # 챗봇 패널 (슬라이드)
│       ├── ChatBotTab.jsx         # Assistant / Agent 탭 전환
│       └── ChatBot.css
├── pages/
│   ├── Overview.jsx               # 메인 (KPI + 트렌드 + 미니맵 + Top N)
│   ├── WaferMap.jsx               # 웨이퍼맵 상세
│   ├── LocationAnalysis.jsx       # 위치 분석 (lot → wafer → position)
│   ├── FeatureAnalysis.jsx        # 변수 분석 (SHAP + 분포)
│   ├── GroupCompare.jsx           # 그룹 비교
│   └── DataTable.jsx              # 데이터 테이블
└── hooks/
    └── useCSV.js                  # CSV 로드 + 파싱
```

---

## 7. 디자인 방향

### 컬러 팔레트
| 역할 | 색상 | 코드 |
|------|------|------|
| Primary (사이드바, 강조) | 남색 | `#1E3A5F` |
| Secondary (버튼, 포인트) | 밝은 파랑 | `#3B82F6` |
| Accent (호버, 하이라이트) | 하늘색 | `#60A5FA` |
| 배경 | 흰색/연회색 | `#F8FAFC` |
| 카드 배경 | 흰색 | `#FFFFFF` |
| 위험 (불량) | 빨강 | `#EF4444` |
| 경고 | 주황 | `#F97316` |
| 정상 | 초록 | `#22C55E` |
| 텍스트 기본 | 짙은 회색 | `#1E293B` |
| 텍스트 보조 | 회색 | `#64748B` |

### 디자인 원칙
- 라이트 테마 (흰색 배경, 남색 사이드바)
- 카드 기반 레이아웃 (그림자 + 라운드 모서리)
- 한 화면에 필요한 정보만 깔끔하게
- 호버 시 카드 테두리 강조 + 그림자 증가

---

## 8. 기술 스택

| 분류 | 선택 | 이유 |
|------|------|------|
| 빌드 도구 | Vite | 빠른 HMR |
| 프레임워크 | React 18 | 컴포넌트 재사용 |
| 언어 | JavaScript (JSX) | 진입장벽 낮음 |
| UI 라이브러리 | Ant Design | 테이블/필터/레이아웃 풍부 |
| 차트 | ECharts (echarts-for-react) | 히트맵, 바차트, 라인 모두 지원 |
| 스타일 | CSS Modules + CSS 변수 | 전역 테마 관리 |
| 데이터 파싱 | PapaParse | CSV → JSON |
| 상태 관리 | useState / useContext | 초기 충분, 복잡해지면 Zustand |
| 배포 | Vercel | GitHub 연동, 무료 |

---

## 9. 최종 모델 확정 (2026-05-21)

### 예측 모델

| 항목 | 내용 |
|------|------|
| **최종 모델** | **15개 base 모델 스태킹 앙상블 (Ridge + Isotonic Regression)** |
| 결과 경로 | `4_output/04_stacking/results_extreme_v2/run_0518_222614` |
| 선택 기준 | `min(oof_rmse)`, val_gap_penalty=2.0 |
| Feature Importance | ZITboost(zit_only) 기준만 사용 |
| SHAP 분석 | ZITboost(zit_only) 기준만 사용 (shap_bar.csv, shap_beeswarm.csv) |
| 예측값 컬럼 | `reg_pred` — 회귀 예측 (메인), `clf_proba` — ZIT 내부 π(x) 참고용 |
| 베이스라인 RMSE | 0.0150 (SK Hynix 사내 경진대회 최우수) |

### 최종 모델 성능

| 기준 | RMSE | 베이스라인 대비 |
|------|------|----------------|
| **OOF RMSE** | **0.005483** | -63.5% |
| **Val RMSE** | **0.005698** | -62.0% |
| **Test RMSE** | **0.008405** | -44.0% |

### 스태킹 구성 (15개 base 모델)

| # | 모델명 | 카테고리 |
|---|--------|----------|
| 1 | 01_zit__zit_only__pphp__001 | ZITboost (zit_only) |
| 2 | 01_zit__zit_only__raw__001 | ZITboost (zit_only) |
| 3 | 01_zit__zit_only__hp__001 | ZITboost (zit_only) |
| 4 | 01_zit__bag_zit__pphp__001 | ZITboost (bag_zit) |
| 5 | 01_zit__bag_zit__hp__001 | ZITboost (bag_zit) |
| 6 | 01_zit__bag_zit__raw__001 | ZITboost (bag_zit) |
| 7 | 03_two_stage__reverse__hp__001 | Two-Stage (reverse) |
| 8 | 03_two_stage__default__clf__xgb__hp__001 | Two-Stage (XGBoost clf) |
| 9 | 03_two_stage__default__clf__xgb__hp__002 | Two-Stage (XGBoost clf) |
| 10 | 03_two_stage__default__clf__lgbm__hp__002 | Two-Stage (LGBM clf) |
| 11 | 03_two_stage__default__clf__catboost__hp__001 | Two-Stage (CatBoost clf) |
| 12 | 03_two_stage__default__clf__catboost__raw__001 | Two-Stage (CatBoost clf) |
| 13 | 02_reg_single__lgbm__hp__001 | 단일 회귀 (LGBM) |
| 14 | 02_reg_single__xgb__hp__002 | 단일 회귀 (XGBoost) |
| 15 | 02_reg_single__et__hp__001 | 단일 회귀 (ExtraTrees) |

- **meta-learner**: Ridge (alpha=1e-9) + Isotonic Regression (iso_weight=1.0)
- **구성 비율**: ZITboost 계열 7개 / Two-Stage 5개 / 단일 회귀 3개

### 위험 등급 기준 (현재 사용 중)

```
Grade 1 (안전):     reg_pred < g1 (p50)         — train 전체의 하위 50%
Grade 2 (관찰):     g1 ≤ reg_pred < g2 (p70.8)  — p50 ~ p70.8 구간
Grade 3 (고위험):   g2 ≤ reg_pred < g3 (p99.95) — p70.8 ~ p99.95 구간
Grade 4 (위험):     reg_pred ≥ g3 (p99.95)      — 상위 0.05%, 임계 초과
```

- 임계값 계산: `computeThresholds(units)` in Overview.jsx — train split 기준 동적 계산
- **검토 중 대안 (IQR 기반)**:
  ```
  Grade 1 (정상):     reg_pred < Q2
  Grade 2 (조심):     Q2 ≤ reg_pred < Q3
  Grade 3 (위험):     Q3 ≤ reg_pred < Q3 + 1.5×IQR
  Grade 4 (매우위험): reg_pred ≥ Q3 + 1.5×IQR
  ```
  → 실데이터 분포 확인 후 현재 기준과 비교하여 최종 결정 예정

---

## 10. 데이터 구조

### 실제 사용 파일 (public/) — 2026-05-21 기준

```
public/
├── dashboard_units.csv      → unit-level 예측 결과 (메인 데이터)
│                              컬럼: ufs_serial, run_id, wafer_no, date, split,
│                                     health, clf_proba, reg_pred, risk, grade
├── wafer_map.csv            → die-level 실좌표 + 예측 (웨이퍼맵/Drilldown용)
│                              컬럼: ufs_serial, run_id, wafer_no, die_x, die_y,
│                                     position, split, health, clf_proba, pred, risk
├── trend_data.csv           → 날짜별 y_pred, y_true, production (트렌드 차트용)
├── feature_importance.csv   → feature별 중요도 (zit_only 기준)
│                              컬럼: feature, lgbm_gain, et_gain, enet_coef, 순위들
├── shap_bar.csv             → feature별 SHAP 평균 기여도 (zit_only 기준)
│                              컬럼: feature, mean_shap, mean_abs_shap
├── shap_beeswarm.csv        → unit별 feature SHAP 값 (Overview SHAP Top10용)
│                              컬럼: ufs_serial, feature, shap_value
├── feature_dist.csv         → unit별 주요 feature 값 (분포/산점도용)
│                              컬럼: ufs_serial, health, is_defect, X...(주요 feature들)
├── metrics.csv              → 모델 성능 지표
│                              컬럼: stage, model, split, metric, value
└── oof_meta.csv             → OOF 예측 원본
```

---

### 대시보드용 날짜·Lot·Wafer 재매핑 (2026-05-04)

**배경**: 원본 데이터는 5일치를 수집 후 같은 lot/wafer 안에서 6:2:2 random split.
train/val이 시간 차이 없이 동일 생산 배치에 섞여 있어 시계열 서사가 불가능했음.

**해결**: 모델 예측값(clf_proba, reg_pred)은 그대로 유지하고, 대시보드 표시용 lot/wafer/날짜만 재매핑.

```
오늘: 2026-06-11 (대시보드 기준일)
어제: 2026-06-10 = 최신 val lot 56 = KPI 기준일

train: lot  1~28  / 2026-03-27 ~ 2026-05-11 (46일 구간)
val:   lot 29~56  / 2026-05-12 ~ 2026-06-10 (하루 1 lot, 30일치)
test:  lot 57~84  / 2026-06-11 이후 (대시보드 미표시)

비율: train 60% / val 20% / test 20% = 6:2:2 유지
서사: train = 과거 생산 완료분 (Y값 확인됨)
      val   = 최근 생산분, 어제까지 (아직 Y값 없음, 예측 대상)
      test  = 오늘 이후 (미래, 대시보드 트렌드에서 제외)
```

> **주의**: lot/wafer/날짜는 대시보드 시각화용으로 재구성된 값. 모델 학습에 사용된 원본 run_id와 다름.

---

### dashboard_units.csv (43,745 rows — unit 단위)
| 컬럼 | 설명 | 비고 |
|---|---|---|
| `ufs_serial` | unit 식별자 | S00000~ |
| `run_id` | Lot 번호 (재매핑) | train 1~28 / val 29~56 / test 57~84 |
| `wafer_no` | Wafer 번호 (재매핑) | split 내 1~25 재번호 |
| `date` | 생산 날짜 (재매핑) | lot 단위로 날짜 부여 |
| `split` | train / val / test | |
| `health` | 실제 불량 값 | train만 의미 있음, val/test = 미공개 |
| `clf_proba` | 불량 확률 (Stage 1 분류기) | 화면 표현: 위험도 |
| `reg_pred` | 예측 불량지수 (Stage 2 회귀) | 화면 표현: 예측 불량지수 |
| `risk` | HIGH / MED / LOW | reg_pred 기준 (상위 10% → HIGH / 불량 threshold 이상 → MED / 미만 → LOW) |

---

### wafer_map.csv (174,572 rows — die 단위)
| 컬럼 | 설명 | 비고 |
|---|---|---|
| `ufs_serial` | unit 식별자 | |
| `run_id` | Lot 번호 | run_wf_xy 파싱 |
| `wafer_no` | Wafer 번호 | run_wf_xy 파싱 |
| `die_x` | die X 좌표 (실좌표) | run_wf_xy 파싱 |
| `die_y` | die Y 좌표 (실좌표) | run_wf_xy 파싱 |
| `position` | die의 unit 내 위치 (1~4) | |
| `split` | train / val / test | |
| `health` | 실제 불량 값 | train만 의미 있음 |
| `clf_proba` | 불량 확률 | |
| `reg_pred` | 예측 불량지수 | |
| `risk` | HIGH / MED / LOW | |

---

### feature_importance.csv (feature 단위)
| 컬럼 | 설명 |
|---|---|
| `feature` | feature명 (X0~X1086) |
| `lgbm_gain` | LGBM gain 기준 importance |
| `et_gain` | ExtraTrees impurity 기준 importance |
| `enet_coef` | ElasticNet 계수 절대값 |
| `lgbm_rank` | LGBM importance 순위 |
| `et_rank` | ET importance 순위 |
| `enet_rank` | ElasticNet 순위 |

---

### metrics.csv (모델 성능 지표)
| 컬럼 | 설명 |
|---|---|
| `stage` | reg (회귀) / clf (분류) |
| `model` | lgbm / et / enet |
| `split` | val / test / train |
| `metric` | rmse / auc / recall / ap 등 |
| `value` | 수치 |

---

## 11. 참고 사항

- **현업 원칙**: 입력은 WT 측정값(X0~X1086) + position + die 좌표만 사용. health 기반 피처 미사용
- **grade threshold**: train split reg_pred 기준 p50/p70.8/p99.95 분위수 (대시보드 로드 시 동적 계산). health 실제값 없이 새 배치에도 적용 가능한 현업 기준.
- **feature 이름**: X0~X1086 비식별화 → SHAP/Importance 상위 feature는 코드명 그대로 표시
- **Agent**: localhost:8000 백엔드 연동 구현됨 (ChatBot.jsx). 더미 응답에서 실제 API 연동으로 전환 예정
- **모델 분석 기준**: Feature Importance, SHAP 모두 ZITboost(zit_only) 계열 기준. 15개 스태킹 ensemble 전체의 해석이 아님을 발표 시 명시 필요
