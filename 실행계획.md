# SK Hynix 위험 Unit 사전 탐지 시스템 — 분석 시스템 실행 계획

**주제**: 반도체 특성 기반 품질 불량 예측 시스템
**설명**: 품질 불량 위험이 높은 반도체를 사전에 식별하는 예측 모델링 및 품질 분석 시스템 구축, AI 활용 의사 결정 지원

> 작성일: 2026-04-23 | 갱신: 2026-05-28
> 최종 발표: 2026-06-11

---

## 0. 프로젝트 목적 & 사용자

### 배경
반도체 생산에서 Field 불량은 **WT 완료 후 수개월이 지나야** 확인된다.

```
WT 완료 → PKG(15일) → PKT(15일) → 현장 투입 → health 수집(1~3개월 소요)
```

이 시간 동안 불량 위험 unit은 이미 출하된 상태이며, 문제를 인지했을 때는
손쓸 수 없는 상황이 된다. **WT 측정값(X0~X1086)으로 미리 위험을 탐지**하는 것이 이 시스템의 존재 이유다.

---

### 목적

> **WT 단계에서 Field 불량 위험이 높은 unit을 사전에 식별하고,
> PI가 의사결정할 수 있도록 시각화 + AI 분석을 제공한다.**

구체적으로 세 가지를 답한다:

| 질문 | 답 |
|------|-----|
| **지금 위험한 unit이 뭔가?** | Val/Test 예측 결과 기반 위험 unit 목록 |
| **왜 위험한가?** | 주요 불량 유발 Feature 분석 |
| **어디서 났는가?** | Lot / Wafer / Position / 웨이퍼맵 |

---

### 데이터 시나리오 (핵심)

이 시스템은 **두 가지 데이터 상태**를 동시에 다룬다.

```
Train  →  WT 값 + 실제 health(Y) 모두 있음
           "과거 생산분 — 불량 패턴이 이미 확인된 데이터"
           역할: 모델 학습 + 패턴 파악

Val    →  WT 값만 있음, health(Y)는 아직 없음
           "방금 생산된 제품 — 아직 Field 결과가 안 나온 상태"
           역할: 실제 예측 대상 (PI가 지금 봐야 하는 것)

Test   →  Val과 동일한 상태 (최종 제출용)
```

> **대시보드의 메인은 Val이다.**
> "지금 이 unit들이 위험한가?"에 답하는 것이 핵심.
> Train은 "왜 이 패턴이 위험한지" 설명하는 보조 역할.

---

### 표현 원칙

모델 성능 지표(RMSE, AUC, clf_proba 수치)는 전면에 노출하지 않는다.
PI가 알아야 하는 것은 **"이 unit이 위험한가, 얼마나, 왜"**이지 모델 평가 지표가 아니다.

| 내부 표현 | 화면 표현 |
|----------|----------|
| Grade 4 (reg_pred ≥ Q3+1.5×IQR) | 🔴 매우위험 (G4) |
| Grade 3 (Q3 ≤ reg_pred < Q3+1.5×IQR) | 🟠 위험 (G3) |
| Grade 2 (Q2 ≤ reg_pred < Q3) | 🟡 조심 (G2) |
| Grade 1 (reg_pred < Q2) | 🟢 정상 (G1) |
| reg_pred 수치 × 1,000,000 | 예측 불량 ppm |
| health > 0 (Train) | 실제 불량 확인 |
| RMSE, AUC 등 | ML 모델 분석 탭에서만 (별도 분리) |

> **위험 등급 기준 (현재 확정: 전체 reg_pred IQR 기반, 동적 계산)**
> - **Q2 = p50** (Grade 1/2 경계) — 전체 중앙값
> - **Q3 = p75** (Grade 2/3 경계)
> - **Q3+1.5×IQR** (Grade 3/4 경계) — IQR 울타리 초과 = 매우위험
> - 대시보드 로드 시 전체 데이터 기준으로 동적 계산 (`computeThresholds()` in Overview.jsx)
>
> **최종 예측 모델**: 15개 base 모델 스태킹 앙상블
> - Feature Importance, SHAP 분석은 **ZITboost 계열 모델(zit_only)** 기준으로만 산출
> - clf_proba: ZITboost 내부 π(x) — 참고용으로만 유지, 위험 분류 기준은 reg_pred 사용

---

### 사용자
- **1차**: PI (Process Integration 엔지니어) — 수율 점검, 불량 원인 파악, 공정 개선
- **2차**: 포트폴리오 열람자 — "현업 수준의 ML 시스템을 설계·구현했다"는 것을 보여주는 결과물

### PI 의사결정 흐름
```
① 위험 현황 파악   →  불량 현황 / 불량 현황 Ⅱ (지금 위험한 unit 몇 개?)
② 위치 파악        →  불량 상세 분석 (어느 Lot, 어느 위치?)
③ 원인 파악        →  ML 모델 분석 (왜 위험한가?)
④ 조치 결정        →  AI Agent (어떻게 해야 하나?)
⑤ 보고서 작성      →  AI Agent (보고서 자동 생성)
```

---

## 1. 대시보드 vs Agent 역할 분리

### 핵심 원칙
```
대시보드 = "무슨 일이 일어나고 있나"  →  숫자와 차트로 현상 시각화
Agent    = "그래서 어떻게 해야 하나"  →  원인 분석 + 조치 제안 + 보고서 생성
```

### 대시보드가 답하는 것 (정적 시각화)
| PI 질문 | 대응 화면 |
|---|---|
| 전체 grade 분포가 어떻게 돼? | 불량 현황 — 도넛 차트 3개 + KPI + 주차별 트렌드 |
| Grade 4가 몇 개야? | 불량 현황 Ⅱ — KPI 카드 + 위험 Lot 순위 |
| 어느 lot에서 많이 났어? | 불량 현황 Ⅱ — 위험 Lot 순위표 (클릭 → 불량 상세 분석) |
| 어느 die 위치에서 불량이 났어? | 불량 상세 분석 — Lot 트리 + 웨이퍼맵 + Unit 진단 |
| 특정 lot/wafer를 파고 보고 싶어 | 불량 상세 분석 — 좌측 트리 드릴다운 |
| 모델이 왜 이렇게 판단했어? | ML 모델 분석 — FI + SHAP + 분포 + 바이올린 |

### Agent가 답하는 것 (동적 추론)
| PI 질문 | Agent 기능 |
|---|---|
| 왜 이번 lot A에서 특히 많이 났어? | 원인 분석 |
| 이 패턴 이전에도 있었어? | 패턴 비교 |
| X005가 높으면 어떻게 조치해야 해? | 조치 제안 |
| 개선조치 보고서 써줘 | 보고서 자동 생성 |
| 앞으로 어떻게 막아? | 방어선 제안 |

---

## 2. 전체 레이아웃

```
┌─────────────────────────────────────────────────────────┐
│  TopBar: SK hynix 로고 / 날짜 / 알림 🔔 / 프로필        │
├─────────────┬───────────────────────────────────────────┤
│             │                                           │
│  좌측       │  메인 콘텐츠 영역                          │
│  사이드바   │  (선택 메뉴에 따라 변경)                   │
│  (접이식)   │                                           │
│  ◈ 불량현황 │                                           │
│  ◈ 불량현황Ⅱ│                                           │
│  ◉ 불량상세 │                          [💬 챗봇 버튼]   │
│  ◇ ML분석  │                                           │
│             │                                           │
│  [프로필]   │                                           │
└─────────────┴───────────────────────────────────────────┘
```

- 사이드바: 로고 버튼 클릭으로 펼침/접힘, 접힌 상태에서 아이콘+툴팁
- 챗봇 FAB: 우측 하단 고정 (💬), 클릭 시 ChatBot 패널 열림 / 열린 상태에서 FAB 숨김

---

## 3. 사이드바 메뉴 구성 (현재 확정)

```javascript
// Sidebar.jsx MENU 배열
const MENU = [
  { id: 'overview',        label: '불량 현황',     icon: '◈' },
  { id: 'overview2',       label: '불량 현황 Ⅱ',  icon: '◈' },
  { id: 'drilldown',       label: '불량 상세 분석', icon: '◉' },
  { id: 'feat-importance', label: 'ML 모델 분석',  icon: '◇' },
]
```

| 메뉴 ID | 레이블 | 페이지 컴포넌트 |
|---------|--------|---------------|
| `overview` | 불량 현황 | Overview.jsx |
| `overview2` | 불량 현황 Ⅱ | Overview2.jsx |
| `drilldown` | 불량 상세 분석 | Drilldown.jsx |
| `feat-importance` | ML 모델 분석 | ModelPerformance.jsx |

> **제거된 계획 페이지** (미구현): 위치 분석(Position/Lot/Wafer 탭), 그룹 비교, 데이터 테이블 별도 탭
> **WaferMap.jsx**: 독립 탭으로도 존재하나 현재 사이드바에서 제외됨 (Drilldown 내부에서 웨이퍼맵 기능 포함)

---

## 4. 페이지별 구성 (현재 구현 기준)

### 📊 불량 현황 (Overview.jsx) — ✅ 구현 완료

**"주차별 전체 품질 트렌드를 한눈에"**

**KPI 카드 3개 (가로 한 줄)**:
- **이번주 검사 유닛** — 전체 unit 수 (`units.length`)
- **이번달 품질 실적** — 최근 4주 평균 예측 ppm
- **두달 뒤 예측 (WW37)** — 전체 유닛 reg_pred 평균 × 1,000,000

**도넛 차트 3개 (가로 한 줄, pie-card)**:
- **전체 Grade 분포** — 전체 데이터 기준 G1/G2/G3/G4 비율
- **최근 4주 Grade 분포** — trendRaw 마지막 28일 날짜 기준 유닛
- **WW37 예측 위험도** — 전체 유닛 grade 기준

**주차별 불량 ppm 트렌드 차트** (trend_data.csv):
- x축: WW 주차 레이블 (WW20~WW37)
- 이중 y축: 좌측=생산량 막대, 우측=불량 ppm 꺾은선
- 3구간 배경: 실측(회색) / 예측(파랑) / 최신주차(빨강 점선)
- WW37 markPoint: 예측값 표시

---

### 📊 불량 현황 Ⅱ (Overview2.jsx) — ✅ 구현 완료

**"위험 Lot 중심의 현황 요약"**

**KPI 카드 4개 (가로 한 줄)**:
- **고위험 유닛 (Grade 4)** — grade4 count, 0이면 초록, 1+ 이면 빨강
- **이번주차 검사 완료 유닛** — 전체 unit 수
- **평균 예측 PPM** — 전체 reg_pred 평균 × 1e6
- **정상 유닛 비율 (G1)** — grade1 비율%, 70% 이상 초록 / 미만 주황

**중간 행 (2열)**:
- 좌: **위험 Lot 순위 (G3+G4 비율 기준 Top 10)** 테이블
  - 컬럼: 순위 / Lot번호 / 위험비율(바차트+%) / G3수 / G4수 / avgPPM / 전체
  - 행 클릭 → `onNavigateDrilldown({ lot })` → 불량 상세 분석으로 이동
- 우: **Grade 분포 도넛 차트** (범례 우측에 퍼센트 표시)

**주차별 불량 ppm 트렌드 차트** — Overview와 동일 구조

---

### 🔬 불량 상세 분석 (Drilldown.jsx) — ✅ 구현 완료

**"특정 Lot/Wafer/Unit/Die를 파고들어 원인 파악"**

- **좌측 Lot 트리**: Lot 목록 (위험률 정렬) → 클릭 시 해당 Lot의 Wafer 목록 펼침
- **중앙 WaferMap SVG**: 선택된 Wafer의 die 히트맵 (색상 = pred 크기)
  - 정상=파랑계열(`NORMAL_STOPS`), 위험=노랑→빨강(`RISK_STOPS`)
  - threshold: Q3 기준 동적 계산
- **우측 Unit 진단**: 선택된 Unit의 verdict(위험/정상) + 예측 ppm + grade 표시
- **탭**: Lot 탭 / Wafer 탭 / Unit 탭 / Die 탭
- 불량 현황 Ⅱ 위험 Lot 테이블에서 행 클릭 시 해당 Lot으로 직접 이동 (`initialSelection` prop)
- 데이터: wafer_map.csv + dashboard_units.csv

**WaferMap.jsx** (독립 웨이퍼맵, 사이드바 미노출):
- 날짜 선택 → Lot 선택 → Wafer 선택 → die 히트맵
- 웨이퍼 좌표 정규화: WAFER_CX=39, WAFER_CY=21.5, WAFER_RX=27, WAFER_RY=10.5

---

### 📈 ML 모델 분석 (ModelPerformance.jsx) — ✅ 구현 완료

**"모델이 왜 이렇게 판단했는가"**

**KPI 행 2개**:
- **앙상블 모델 검증 오차(RMSE)** — stacking 또는 ensemble val RMSE (`BASELINE_RMSE = 0.005845`)
- **사내경진대회 대비 개선** — 베이스라인 대비 개선율(%)

**피처 중요도 · SHAP 분석** (스크롤 가능 카드, 2열 분할):
- 좌: **피처 중요도 Top20** (feature_importance.csv lgbm_gain 기준, 막대 클릭 → 피처 선택)
- 우: **SHAP 분석 Beeswarm** (shap_beeswarm.csv, 빨강=불량증가/파랑=불량감소 방향)
- 피처 클릭 시 선택 연동 (FI ↔ Beeswarm 양방향), 선택 피처는 보라색(`#7C3AED`) 강조

**하단 2열 그리드**:
- 좌: **피처 분포** — 선택 feature의 Grade1·2(파랑) vs Grade3·4(빨강) 밀도 라인 (feature_dist.csv)
- 우: **피처 일별 분포** — 선택 feature의 날짜별 바이올린 플롯 (feature_violin.json, 커스텀 renderItem)

> **중요**: Feature Importance와 SHAP 분석은 모두 **ZITboost 계열 모델(zit_only) 기준**으로만 산출.
> 15개 스태킹 ensemble 전체의 해석이 아님을 발표 시 명시 필요.

---

## 5. 챗봇 (AI Agent) 구성

### 열기 방식
- 우측 하단 고정 버튼 (💬) — `chatbot-fab` 클래스
- 클릭 시 ChatBot.jsx 패널 열림
- 챗봇 열린 상태에서 FAB 숨김 (`{!chatOpen && <button ... />}`)

### 챗봇 기능
- 현재: 더미 응답 + localhost:8000 백엔드 연동 준비
- 실제 API 연동으로 전환 예정

---

## 6. 컴포넌트 구조 (실제 파일 기준)

```
5_분석시스템/
├── 실행계획.md
├── agent/
│   ├── pptx_spec.md
│   └── REPORT_LAYOUT.md
└── dashboard/
    ├── index.html
    ├── package.json
    ├── vite.config.js
    ├── public/                      ← CSV/JSON 데이터 파일 위치
    │   ├── dashboard_units.csv
    │   ├── wafer_map.csv
    │   ├── trend_data.csv
    │   ├── feature_importance.csv
    │   ├── shap_bar.csv
    │   ├── shap_beeswarm.csv
    │   ├── feature_dist.csv
    │   ├── feature_violin.json
    │   └── metrics.csv
    └── src/
        ├── main.jsx
        ├── App.jsx                  # 라우팅 + 전역 상태
        ├── App.css
        ├── index.css
        ├── assets/
        │   ├── sk_logo_nobg.png
        │   └── sk_logo.webp
        ├── components/
        │   ├── TopBar.jsx / .css    # 상단 바 (로고, 날짜, 알림🔔, 프로필)
        │   ├── Sidebar.jsx / .css   # 좌측 네비게이션 (접이식)
        │   ├── ChatBot.jsx / .css   # 챗봇 패널
        │   ├── PageHeader.jsx / .css
        │   └── ReportModal.jsx / .css
        ├── pages/
        │   ├── Overview.jsx / .css        # 불량 현황
        │   ├── Overview2.jsx / .css       # 불량 현황 Ⅱ
        │   ├── WaferMap.jsx / .css        # 웨이퍼맵 (독립)
        │   ├── Drilldown.jsx / .css       # 불량 상세 분석
        │   ├── diePositions.js            # die 좌표 사전 데이터
        │   └── ModelPerformance.jsx / .css # ML 모델 분석
        └── hooks/
            └── useCSV.js                  # CSV 로드 + 파싱 (PapaParse)
```

### App.jsx 핵심 상태
```javascript
const [activePage, setActivePage] = useState('overview')
const [chatOpen, setChatOpen] = useState(false)
const [pendingSelection, setPendingSelection] = useState(null)  // { lot, wafer, unit }

// Overview2 위험Lot 클릭 → Drilldown으로 이동하면서 selection 전달
const navigateToDrilldown = (selection) => {
  setPendingSelection(selection)
  setActivePage('drilldown')
}
```

---

## 7. 디자인 방향

### 컬러 팔레트
| 역할 | 색상 | 코드 |
|------|------|------|
| Primary (사이드바, 강조) | 남색 | `#1E3A5F` |
| Secondary (버튼, 포인트) | 밝은 파랑 | `#3B82F6` |
| Accent (호버, 하이라이트) | 하늘색 | `#60A5FA` |
| 배경 | 흰색/연회색 | `#F8FAFC` |
| 카드 배경 | 흰색 | `#FFFFFF` |
| Grade 4 (매우위험) | 빨강 | `#EF4444` |
| Grade 3 (위험) | 주황/노랑 | `#F59E0B` |
| Grade 2 (조심) | 노랑 | `#EAB308` |
| Grade 1 (정상) | 초록 | `#22C55E` |
| 텍스트 기본 | 짙은 회색 | `#1E293B` |
| 텍스트 보조 | 회색 | `#64748B` |

### 디자인 원칙
- 라이트 테마 (흰색 배경, 남색 사이드바)
- 카드 기반 레이아웃 (그림자 + 라운드 모서리)
- 한 화면에 필요한 정보만 깔끔하게
- 사이드바 접이식 — 아이콘만 보이는 좁은 모드 + 레이블 보이는 넓은 모드

---

## 8. 기술 스택

| 분류 | 선택 | 이유 |
|------|------|------|
| 빌드 도구 | Vite | 빠른 HMR |
| 프레임워크 | React 18 | 컴포넌트 재사용 |
| 언어 | JavaScript (JSX) | 진입장벽 낮음 |
| 차트 | ECharts (echarts-for-react) | 히트맵, 바차트, 라인 모두 지원 |
| 스타일 | CSS (컴포넌트별 .css 파일) | 전역 테마 관리 |
| 데이터 파싱 | PapaParse (useCSV.js) | CSV → JSON |
| 상태 관리 | useState / useMemo | 현재 충분 |
| 배포 | Vercel | GitHub 연동, 무료 |

---

## 9. 최종 모델 확정 (2026-05-21)

### 예측 모델

| 항목 | 내용 |
|------|------|
| **최종 모델** | **15개 base 모델 스태킹 앙상블 (Ridge + Isotonic Regression)** |
| 결과 경로 | `4_output/04_stacking/results_extreme_v2/run_0518_222614` |
| 선택 기준 | `min(oof_rmse)`, val_gap_penalty=2.0 |
| Feature Importance | ZITboost(zit_only) 기준만 사용 |
| SHAP 분석 | ZITboost(zit_only) 기준만 사용 (shap_bar.csv, shap_beeswarm.csv) |
| 예측값 컬럼 | `reg_pred` — 회귀 예측 (메인), `clf_proba` — ZIT 내부 π(x) 참고용 |
| 베이스라인 RMSE | 0.005845 (대시보드 내 비교 기준) |

### 최종 모델 성능

| 기준 | RMSE | 베이스라인 대비 |
|------|------|----------------|
| **OOF RMSE** | **0.005483** | -6.2% |
| **Val RMSE** | **0.005698** | -2.5% |
| **Test RMSE** | **0.008405** | +43.8% |

### 스태킹 구성 (15개 base 모델)

| # | 모델명 | 카테고리 |
|---|--------|----------|
| 1 | 01_zit__zit_only__pphp__001 | ZITboost (zit_only) |
| 2 | 01_zit__zit_only__raw__001 | ZITboost (zit_only) |
| 3 | 01_zit__zit_only__hp__001 | ZITboost (zit_only) |
| 4 | 01_zit__bag_zit__pphp__001 | ZITboost (bag_zit) |
| 5 | 01_zit__bag_zit__hp__001 | ZITboost (bag_zit) |
| 6 | 01_zit__bag_zit__raw__001 | ZITboost (bag_zit) |
| 7 | 03_two_stage__reverse__hp__001 | Two-Stage (reverse) |
| 8 | 03_two_stage__default__clf__xgb__hp__001 | Two-Stage (XGBoost clf) |
| 9 | 03_two_stage__default__clf__xgb__hp__002 | Two-Stage (XGBoost clf) |
| 10 | 03_two_stage__default__clf__lgbm__hp__002 | Two-Stage (LGBM clf) |
| 11 | 03_two_stage__default__clf__catboost__hp__001 | Two-Stage (CatBoost clf) |
| 12 | 03_two_stage__default__clf__catboost__raw__001 | Two-Stage (CatBoost clf) |
| 13 | 02_reg_single__lgbm__hp__001 | 단일 회귀 (LGBM) |
| 14 | 02_reg_single__xgb__hp__002 | 단일 회귀 (XGBoost) |
| 15 | 02_reg_single__et__hp__001 | 단일 회귀 (ExtraTrees) |

- **meta-learner**: Ridge (alpha=1e-9) + Isotonic Regression (iso_weight=1.0)
- **구성 비율**: ZITboost 계열 6개 / Two-Stage 6개 / 단일 회귀 3개

### 위험 등급 기준 (IQR 기반 확정)

```
Grade 1 (정상):     reg_pred < Q2             — 전체의 하위 50%
Grade 2 (조심):     Q2 ≤ reg_pred < Q3        — Q2~Q3 구간
Grade 3 (위험):     Q3 ≤ reg_pred < Q3+1.5IQR — Q3 이상 울타리 미만
Grade 4 (매우위험): reg_pred ≥ Q3+1.5IQR      — 울타리 초과, 극단치
```

- 임계값 계산: `computeThresholds(units)` in Overview.jsx
- Q1 = p25, Q2 = p50, Q3 = p75, IQR = Q3 - Q1, upperFence = Q3 + 1.5 × IQR
- 전체 dashboard_units.csv reg_pred 기준 동적 계산 (train/val/test 구분 없이 전체)

---

## 10. 데이터 구조

### 실제 사용 파일 (public/) — 2026-05-28 기준

```
public/
├── dashboard_units.csv      → unit-level 예측 결과 (메인 데이터)
│                              컬럼: ufs_serial, run_id, wafer_no, date, split,
│                                     health, clf_proba, reg_pred, risk, grade
├── wafer_map.csv            → die-level 실좌표 + 예측 (웨이퍼맵/Drilldown용)
│                              컬럼: ufs_serial, run_id, wafer_no, die_x, die_y,
│                                     position, split, health, clf_proba, pred, risk
├── trend_data.csv           → 날짜별 y_pred, y_true, production (트렌드 차트용)
├── feature_importance.csv   → feature별 중요도 (zit_only 기준)
│                              컬럼: feature, lgbm_gain, et_gain, enet_coef, 순위들
├── shap_bar.csv             → feature별 SHAP 평균 기여도 (zit_only 기준)
│                              컬럼: feature, mean_shap, mean_abs_shap
├── shap_beeswarm.csv        → unit별 feature SHAP 값
│                              컬럼: ufs_serial, feature, shap_value, feat_norm
├── feature_dist.csv         → unit별 주요 feature 값 (분포/산점도용)
│                              컬럼: ufs_serial, health, is_defect, X...(주요 feature들)
├── feature_violin.json      → feature별 날짜별 KDE 바이올린 데이터
│                              형식: [{feature, date, kde:[[y,dens],...], q1, median, q3, mean}, ...]
└── metrics.csv              → 모델 성능 지표
                               컬럼: stage, model, split, metric, value
```

---

### 대시보드용 날짜·Lot·Wafer 재매핑 (2026-05-04)

**배경**: 원본 데이터는 5일치를 수집 후 같은 lot/wafer 안에서 6:2:2 random split.
train/val이 시간 차이 없이 동일 생산 배치에 섞여 있어 시계열 서사가 불가능했음.

**해결**: 모델 예측값(clf_proba, reg_pred)은 그대로 유지하고, 대시보드 표시용 lot/wafer/날짜만 재매핑.

```
오늘: 2026-06-11 (대시보드 기준일)
어제: 2026-06-10 = 최신 val lot 56

train: lot  1~28  / 2026-03-27 ~ 2026-05-11 (46일 구간)
val:   lot 29~56  / 2026-05-12 ~ 2026-06-10 (하루 1 lot, 30일치)
test:  lot 57~84  / 2026-06-11 이후 (대시보드 미표시)

비율: train 60% / val 20% / test 20% = 6:2:2 유지
서사: train = 과거 생산 완료분 (Y값 확인됨)
      val   = 최근 생산분, 어제까지 (아직 Y값 없음, 예측 대상)
      test  = 오늘 이후 (미래, 대시보드 트렌드에서 제외)
```

> **주의**: lot/wafer/날짜는 대시보드 시각화용으로 재구성된 값. 모델 학습에 사용된 원본 run_id와 다름.

---

### dashboard_units.csv (43,745 rows — unit 단위)
| 컬럼 | 설명 | 비고 |
|---|---|---|
| `ufs_serial` | unit 식별자 | S00000~ |
| `run_id` | Lot 번호 (재매핑) | train 1~28 / val 29~56 / test 57~84 |
| `wafer_no` | Wafer 번호 (재매핑) | 1~25 재번호 |
| `date` | 생산 날짜 (재매핑) | lot 단위로 날짜 부여 |
| `split` | train / val / test | |
| `health` | 실제 불량 값 | train만 의미 있음, val/test = 미공개 |
| `clf_proba` | 불량 확률 (ZIT 내부 π(x)) | 참고용 |
| `reg_pred` | 예측 불량지수 | 화면 표현: 예측 불량 ppm (×1e6) |
| `risk` | HIGH / MED / LOW | reg_pred 기준 |
| `grade` | grade1~grade4 | IQR 기준 등급 |

---

### wafer_map.csv (174,572 rows — die 단위)
| 컬럼 | 설명 | 비고 |
|---|---|---|
| `ufs_serial` | unit 식별자 | |
| `run_id` | Lot 번호 | |
| `wafer_no` | Wafer 번호 | |
| `die_x` | die X 좌표 (실좌표) | 12~66 범위 |
| `die_y` | die Y 좌표 (실좌표) | 11~32 범위 |
| `position` | die의 unit 내 위치 (1~4) | |
| `split` | train / val / test | |
| `health` | 실제 불량 값 | train만 의미 있음 |
| `clf_proba` | 불량 확률 | |
| `pred` | 예측 불량지수 (wafer_map 전용 컬럼명) | reg_pred와 동일값 |
| `risk` | HIGH / MED / LOW | |

---

### feature_importance.csv (feature 단위)
| 컬럼 | 설명 |
|---|---|
| `feature` | feature명 (X0~X1086) |
| `lgbm_gain` | LGBM gain 기준 importance |
| `et_gain` | ExtraTrees impurity 기준 importance |
| `enet_coef` | ElasticNet 계수 절대값 |
| `lgbm_rank` | LGBM importance 순위 |
| `et_rank` | ET importance 순위 |
| `enet_rank` | ElasticNet 순위 |

---

### metrics.csv (모델 성능 지표)
| 컬럼 | 설명 |
|---|---|
| `stage` | reg (회귀) / clf (분류) |
| `model` | lgbm / et / enet / stacking / ensemble |
| `split` | val / test / train |
| `metric` | rmse / auc / recall / ap 등 |
| `value` | 수치 |

---

## 11. 참고 사항

- **현업 원칙**: 입력은 WT 측정값(X0~X1086) + position + die 좌표만 사용. health 기반 피처 미사용
- **grade threshold**: 전체 reg_pred IQR 기반 (Q2/Q3/Q3+1.5IQR) — 대시보드 로드 시 동적 계산
- **feature 이름**: X0~X1086 비식별화 → SHAP/Importance 상위 feature는 코드명 그대로 표시
- **Agent**: localhost:8000 백엔드 연동 구현됨 (ChatBot.jsx). 더미 응답에서 실제 API 연동으로 전환 예정
- **모델 분석 기준**: Feature Importance, SHAP 모두 ZITboost(zit_only) 계열 기준. 15개 스태킹 ensemble 전체의 해석이 아님을 발표 시 명시 필요
- **웨이퍼맵 좌표 정규화**: WAFER_CX=39, WAFER_CY=21.5, WAFER_RX=27, WAFER_RY=10.5 기준으로 [-1,1] 정규화 (WaferMap.jsx)
