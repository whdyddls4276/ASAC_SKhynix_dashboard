# SK Hynix 위험 Unit 사전 탐지 시스템 — 대시보드 & AI Agent 실행 계획

**주제**: Wafer Test 기반 위험 Unit 사전 탐지 시스템
**부제**: 품질 예측 대시보드 구축 및 AI를 활용한 의사결정 지원

> 작성일: 2026-04-23 | 갱신: 2026-05-02
> 최종 발표: 2026-06-09

---

## 0. 프로젝트 목적 & 사용자

### 목적
방금 생산된 unit의 WT 측정값만으로 Field 불량 위험을 사전에 탐지하고,
**PI(공정 엔지니어)가 직관적으로 의사결정**할 수 있는 인터랙티브 대시보드 + AI Agent를 구축한다.

> **현업 시나리오**: WT(7일) → PKG(15일) → PKT(15일) → 현장 투입 → health 수집(1~3개월 소요).
> 대시보드는 WT 완료 직후 예측 결과를 즉시 제공하여 불량 위험 unit을 선제적으로 식별한다.

### 사용자
- **PI (Process Integration 엔지니어)**
- 수율 점검, 불량 원인 파악, 공정 개선 의사결정

### PI 의사결정 흐름
```
문제 파악 → 현상 파악 → 연계 분석 → 개선조치 보고서 → 방어선 구축
```

---

## 1. 대시보드 vs Agent 역할 분리

### 핵심 원칙
```
대시보드 = "무슨 일이 일어나고 있나"  →  숫자와 차트로 현상 시각화
Agent    = "그래서 어떻게 해야 하나"  →  원인 분석 + 조치 제안 + 보고서 생성
```

### 대시보드가 답하는 것 (정적 시각화)
| PI 질문 | 대응 화면 |
|---|---|
| 불량 위험 unit이 몇 개야? | Overview KPI 카드 |
| 어제보다 나빠졌어? | 트렌드 차트 |
| 어느 lot/wafer/위치에서 났어? | 웨이퍼맵, 위치 분석 |
| 가장 위험한 unit이 어디야? | 위험 unit Top N 테이블 |
| 왜 불량으로 판단했어? | 변수 분석 (SHAP) |
| 불량/정상 그룹 변수값이 얼마나 달라? | 그룹 비교 |

### Agent가 답하는 것 (동적 추론)
| PI 질문 | Agent 기능 |
|---|---|
| 왜 이번 lot A에서 특히 많이 났어? | 원인 분석 |
| 이 패턴 이전에도 있었어? | 패턴 비교 |
| X005가 높으면 어떻게 조치해야 해? | 조치 제안 |
| 개선조치 보고서 써줘 | 보고서 자동 생성 |
| 앞으로 어떻게 막아? | 방어선 제안 |

---

## 2. 전체 레이아웃

```
┌─────────────────────────────────────────────────────────┐
│  TopBar: 로고 / 날짜 / 알림 🔔 / 프로필                  │
├─────────────┬───────────────────────────────────────────┤
│             │                                           │
│  좌측       │  메인 콘텐츠 영역                          │
│  사이드바   │  (선택 메뉴에 따라 변경)                   │
│             │                                           │
│  📊 Overview│                                           │
│  🗺 웨이퍼맵│                                           │
│  📍 위치분석│                          [🤖 챗봇 버튼]   │
│  🔬 변수분석│                                           │
│  📈 그룹비교│                                           │
│  📋 데이터  │                                           │
└─────────────┴───────────────────────────────────────────┘
```

---

## 3. 메인페이지 (Overview) 구성

**한눈에 현재 상황을 파악하는 페이지**

```
┌─────────────────────────────────────────────────────────┐
│  KPI 카드 3개                                            │
│  [위험 unit 건수]   [불량률 %]   [전일 대비 ▲▼]          │
├──────────────────────────┬──────────────────────────────┤
│  트렌드 차트              │  웨이퍼맵 (미니)              │
│  - 불량률 추이            │  - 전체 위험 분포 한눈에      │
│  - 전일 대비 변화         │  - 클릭 시 웨이퍼맵 페이지로  │
├──────────────────────────┴──────────────────────────────┤
│  위험 unit Top N 테이블                                  │
│  unit명 / lot / wafer / y_pred / risk level             │
└─────────────────────────────────────────────────────────┘
```

### 메인페이지에 넣는 것
- KPI 카드: 위험 unit 건수, 불량률, 전일 대비
- 트렌드 차트: 불량률 추이
- 웨이퍼맵 미니: 전체 위험 분포 (클릭 시 상세 페이지 이동)
- 위험 unit Top N 테이블: unit명 / lot / wafer / y_pred / risk level

### 메인페이지에 넣지 않는 것
- 피처 분석, 그룹 비교 → 사이드바 세부 페이지로 분리
- 모델 성능 (RMSE 등) → PI 불필요, 제거

---

## 4. 사이드바 페이지 구성

### 📊 Overview (메인)
→ 섹션 3 참조

---

### 🗺 웨이퍼맵
**"어느 위치에서 불량이 났는가"**
- lot / wafer 필터
- die_x / die_y 기반 히트맵 (y_pred 색상)
- 위험도 높은 die 위치 강조

---

### 📍 위치 분석
**"어떤 lot/wafer/position이 문제인가"**
- lot별 위험 unit 수 / 불량률 (바차트)
- 선택한 lot → wafer별 드릴다운
- wafer별 → position별 드릴다운
- "어떤 lot이 가장 불량 위험 unit이 많은지, 그 안에서 wafer 간 차이는?"

---

### 🔬 변수 분석
**"왜 불량으로 판단했는가"**
- SHAP Top N 수평 바차트 (불량 영향도)
- Feature Importance 바차트
- 불량/정상 그룹 변수 분포 비교 (밀도 곡선)

---

### 📈 그룹 비교
**"불량/정상 그룹이 얼마나 다른가"**
- 상위 N개 feature의 불량 그룹 평균 vs 정상 그룹 평균 비교
- "X005가 불량 그룹에서 2.3배 높다" 형태로 시각화
- 그룹 간 차이가 큰 변수 순으로 정렬

---

### 📋 데이터 테이블
**"특정 unit 상세 확인"**
- 전체 unit 목록
- risk level / lot / wafer / position 필터
- 정렬 기능 (y_pred 높은 순 등)
- unit 클릭 시 상세 정보 (주요 변수값, SHAP)

---

## 5. 챗봇 (AI Agent) 구성

### 열기 방식
- 우측 하단 고정 버튼 (🤖)
- 클릭 시 오른쪽에서 슬라이드로 패널 열림
- 메인 콘텐츠 영역이 좁아지는 push 방식
- 닫으면 원래대로 복귀

### 탭 구조
```
┌─────────────────────────────┐
│  [  Assistant  ] [  Agent  ] │  ← 탭 클릭으로 전환
├─────────────────────────────┤
│                             │
│  Assistant 탭:              │
│  (더미 — 추후 구현)          │
│                             │
│  Agent 탭:                  │
│  (더미 — 추후 구현)          │
│                             │
├─────────────────────────────┤
│  [입력창          ] [전송]   │
└─────────────────────────────┘
```

### Assistant 탭
- 단순 질문 답변 (더미)
- 예: "불량률이 뭐야?", "SHAP이 뭐야?"

### Agent 탭
- 데이터 기반 분석 + 추론 + 보고서 생성 (더미)
- 예: "이번 lot 왜 불량 많아?", "보고서 써줘"

---

## 6. 컴포넌트 구조

```
src/
├── main.jsx
├── App.jsx                        # 라우팅
├── styles/
│   └── variables.css              # 컬러 변수, 폰트
├── components/
│   ├── layout/
│   │   ├── TopBar.jsx             # 상단 바 (로고, 날짜, 알림, 프로필)
│   │   ├── Sidebar.jsx            # 좌측 네비게이션
│   │   └── Layout.jsx             # 전체 레이아웃 래퍼
│   ├── common/
│   │   ├── KpiCard.jsx            # KPI 숫자 카드
│   │   ├── ChartCard.jsx          # 차트 컨테이너 카드
│   │   └── RiskBadge.jsx          # HIGH / MED / LOW 뱃지
│   ├── charts/
│   │   ├── HeatmapChart.jsx       # 웨이퍼맵 히트맵
│   │   ├── MiniHeatmap.jsx        # Overview용 미니 웨이퍼맵
│   │   ├── TrendChart.jsx         # 불량률 트렌드
│   │   ├── BarChart.jsx           # 위치별 바차트
│   │   ├── ShapChart.jsx          # SHAP 영향도
│   │   ├── DistChart.jsx          # 불량/정상 분포 비교
│   │   └── GroupCompareChart.jsx  # 그룹 비교 바차트
│   └── chatbot/
│       ├── ChatBot.jsx            # 챗봇 패널 (슬라이드)
│       ├── ChatBotTab.jsx         # Assistant / Agent 탭 전환
│       └── ChatBot.css
├── pages/
│   ├── Overview.jsx               # 메인 (KPI + 트렌드 + 미니맵 + Top N)
│   ├── WaferMap.jsx               # 웨이퍼맵 상세
│   ├── LocationAnalysis.jsx       # 위치 분석 (lot → wafer → position)
│   ├── FeatureAnalysis.jsx        # 변수 분석 (SHAP + 분포)
│   ├── GroupCompare.jsx           # 그룹 비교
│   └── DataTable.jsx              # 데이터 테이블
└── hooks/
    └── useCSV.js                  # CSV 로드 + 파싱
```

---

## 7. 디자인 방향

### 컬러 팔레트
| 역할 | 색상 | 코드 |
|------|------|------|
| Primary (사이드바, 강조) | 남색 | `#1E3A5F` |
| Secondary (버튼, 포인트) | 밝은 파랑 | `#3B82F6` |
| Accent (호버, 하이라이트) | 하늘색 | `#60A5FA` |
| 배경 | 흰색/연회색 | `#F8FAFC` |
| 카드 배경 | 흰색 | `#FFFFFF` |
| 위험 (불량) | 빨강 | `#EF4444` |
| 경고 | 주황 | `#F97316` |
| 정상 | 초록 | `#22C55E` |
| 텍스트 기본 | 짙은 회색 | `#1E293B` |
| 텍스트 보조 | 회색 | `#64748B` |

### 디자인 원칙
- 라이트 테마 (흰색 배경, 남색 사이드바)
- 카드 기반 레이아웃 (그림자 + 라운드 모서리)
- 한 화면에 필요한 정보만 깔끔하게
- 호버 시 카드 테두리 강조 + 그림자 증가

---

## 8. 기술 스택

| 분류 | 선택 | 이유 |
|------|------|------|
| 빌드 도구 | Vite | 빠른 HMR |
| 프레임워크 | React 18 | 컴포넌트 재사용 |
| 언어 | JavaScript (JSX) | 진입장벽 낮음 |
| UI 라이브러리 | Ant Design | 테이블/필터/레이아웃 풍부 |
| 차트 | ECharts (echarts-for-react) | 히트맵, 바차트, 라인 모두 지원 |
| 스타일 | CSS Modules + CSS 변수 | 전역 테마 관리 |
| 데이터 파싱 | PapaParse | CSV → JSON |
| 상태 관리 | useState / useContext | 초기 충분, 복잡해지면 Zustand |
| 배포 | Vercel | GitHub 연동, 무료 |

---

## 9. 데이터 구조

### 사용 파일
```
public/
├── dashboard_agg.csv     → unit별 집계 (메인 데이터)
└── shap_summary.csv      → feature별 SHAP 집계
```

### dashboard_agg.csv 컬럼
| 컬럼 | 설명 |
|---|---|
| `ufs_serial` | unit 식별자 |
| `run_id` | lot 번호 |
| `wafer_no` | wafer 번호 |
| `die_x_mean` | unit 내 die X 좌표 평균 |
| `die_y_mean` | unit 내 die Y 좌표 평균 |
| `position` | position (1~4) |
| `split` | train / val / test |
| `y_pred` | 예측 health 값 |
| `is_defect` | y_pred > 0.015 → 1 |
| `risk_level` | high / medium / low |
| `shap_top1_feature` | SHAP 1위 feature |
| `shap_top1_value` | SHAP 1위 값 |

### shap_summary.csv 컬럼
| 컬럼 | 설명 |
|---|---|
| `feature` | feature명 (X0~X1086) |
| `mean_shap_defect` | 불량 그룹 평균 SHAP |
| `mean_shap_normal` | 정상 그룹 평균 SHAP |
| `mean_feature_defect` | 불량 그룹 feature 평균값 |
| `mean_feature_normal` | 정상 그룹 feature 평균값 |
| `importance_rank` | 중요도 순위 |

---

## 10. 참고 사항

- **현업 원칙**: 입력은 WT 측정값(X0~X1086) + position + die 좌표만 사용. health 기반 피처 미사용
- **threshold**: `y_pred > 0.015` 불량 기준
- **feature 이름**: X0~X1086 비식별화 → SHAP 상위 feature는 코드명 그대로 표시
- **Agent**: 더미로 구현 후 추후 Claude API 연동
