# SK Hynix 위험 Unit 사전 탐지 시스템 — 대시보드 & AI Agent 실행 계획

**주제**: Wafer Test 기반 위험 Unit 사전 탐지 시스템
**부제**: 품질 예측 대시보드 구축 및 AI를 활용한 의사결정 지원

> 작성일: 2026-04-23 | 갱신: 2026-04-30
> 중간 발표: 2026-04-28 | 최종 발표: 2026-06-09 | 대시보드 완료 목표: 2026-05-15

---

## 0. 화면 구성 상세 (사용자 요구사항)

### 전체 레이아웃
```
┌─────────────────────────────────────────────────────────┐
│  TopBar: 로고 / Train·Val·Test 모드 전환 / 프로필 / 알림  │
├─────────────┬───────────────────────────────────────────┤
│             │                                           │
│  왼쪽       │  메인 콘텐츠 영역                          │
│  사이드바   │  (선택 메뉴에 따라 변경)                   │
│             │                                           │
│             │                          [챗봇 아이콘 🤖] │
└─────────────┴───────────────────────────────────────────┘
                                         우측 하단 고정
```

### 좌측 사이드바
분석 차원별로 메뉴 분류:
- **상세** (메인/홈)
- **날짜** (일별 / 월별 / 연도별)
- **위치** (Position별 / Unit별 / Zone별)
- **피처 분석** (SHAP / Feature Importance)
- **모델 성능**

### 상단 TopBar
- 좌측: 로고 (`QMS·AI`), 대시보드 타이틀
- 중앙: Train / Val / Test 모드 전환 버튼
- 우측: 날짜 표시, 알림 아이콘 (🔔), **프로필 아이콘 + 이름** (로그인 UI처럼)

### 상세 페이지 (메인/홈)
1. **KPI 카드** (상단)
   - 오늘 불량 건수
   - 불량 발생률 (%)
   - 정상 건수

2. **파이차트 2~3개** (중단)
   - 정상 vs 비정상 비율
   - split별 비율 (train / val / test)
   - risk level별 비율 (high / medium / low)

3. **트렌드 차트** (하단)
   - Train 구간 → **영역차트** (지금까지의 트렌드)
   - Val / Test 구간 → **라인차트** (앞으로의 예측)
   - 하나의 차트에 구간 구분선으로 분리

### 우측 하단 챗봇
- 우측 하단 고정 버튼 (🤖 아이콘)
- 클릭 시 오른쪽에서 챗봇 패널이 슬라이드로 열림
- VS Code 사이드 패널처럼 열리면서 **메인 콘텐츠 영역이 좁아짐** (push 방식)
- 닫으면 메인 영역 원래대로 복귀

---

## 1. 프로젝트 개요

### 목적
방금 생산된 unit의 WT 측정값만으로 Field 불량 위험을 사전에 탐지하고, **PI(공정 엔지니어)가 직관적으로 의사결정**할 수 있는 인터랙티브 대시보드 + AI Agent를 구축한다.

> **현업 시나리오**: WT(7일) → PKG(15일) → PKT(15일) → 현장 투입 → health 수집(1~3개월 소요). 대시보드는 WT 완료 직후 예측 결과를 즉시 제공하여 불량 위험 unit을 선제적으로 식별한다.

### 사용자 (Who)
- **PI (Process Integration 엔지니어)**
- 수율 점검, 불량 원인 파악, 공정 개선 의사결정을 주된 목적으로 사용

### 핵심 질문 (What)
PI가 대시보드에서 얻어야 하는 답:

| # | 질문 | 대응 화면 |
|---|------|----------|
| 1 | 수율이 얼마나 되는지? (불량 개수, 불량률) | Overview KPI 카드 |
| 2 | 불량이 웨이퍼 어느 위치에서 발생했는지? | 웨이퍼맵 히트맵 |
| 3 | 어떤 WT feature가 가장 영향력 있는지? | SHAP / Feature Importance |
| 4 | 불량/정상 그룹의 feature 값은 각각 얼마인지? | 분포 비교 차트 |
| 5 | lot / wafer / position별 위험 분포는? | 필터 + 드릴다운 차트 |

### 주기
- 실제 운영 기준: WT 완료 후 배치 예측 (프로젝트에서는 정적 CSV로 구현)

---

## 2. 디자인 방향

### 레이아웃 구조
```
┌─────────────────────────────────────────────────┐
│  상단 TopBar (로고 / 모드 전환 / 날짜 / 알림)      │
├──────────┬──────────────────────────────────────┤
│          │  KPI 요약 카드 바                      │
│ 왼쪽     ├──────────────────────────────────────┤
│ 사이드바 │                                       │
│ (네비)   │  메인 콘텐츠 영역                      │
│          │  (선택된 메뉴에 따라 차트/테이블 변경)  │
│          │                                       │
└──────────┴──────────────────────────────────────┘
```

### 컬러 팔레트
| 역할 | 색상 | 코드 |
|------|------|------|
| Primary (사이드바, 강조) | 남색 | `#1E3A5F` |
| Secondary (버튼, 포인트) | 밝은 파랑 | `#3B82F6` |
| Accent (호버, 하이라이트) | 하늘색 | `#60A5FA` |
| 배경 | 흰색/연회색 | `#F8FAFC` |
| 카드 배경 | 흰색 | `#FFFFFF` |
| 위험 (불량) | 빨강 | `#EF4444` |
| 경고 | 주황 | `#F97316` |
| 정상 | 초록 | `#22C55E` |
| 텍스트 기본 | 짙은 회색 | `#1E293B` |
| 텍스트 보조 | 회색 | `#64748B` |

### 디자인 원칙
- **라이트 테마** 기반 (흰색 배경, 남색 사이드바)
- 카드 기반 레이아웃 (그림자 + 라운드 모서리)
- 데이터 밀도 높게 — 한 화면에 많은 정보를 깔끔하게
- 모노스페이스 폰트(`DM Mono`)로 수치 표시 → 전문적인 느낌
- 호버 시 카드 테두리 강조 + 그림자 증가

---

## 3. 페이지 구성 (메뉴 구조)

```
대시보드
├── 📊 Overview (홈)
│     KPI 요약 + 트렌드 라인차트 + 불량 unit Top-N 테이블
│
├── 🗺 웨이퍼맵
│     die_x / die_y 기반 히트맵 (y_pred 값으로 색상)
│     lot / wafer 필터
│
├── 📍 위치 분석
│     ├── Position별 불량률 (바차트)
│     ├── Lot별 불량률
│     └── Wafer별 불량률
│
├── 🔬 Feature 분석
│     ├── SHAP Top-N (불량 영향도 바차트)
│     ├── Feature Importance (수평 바차트)
│     └── 불량/정상 분포 비교 (밀도 곡선)
│
└── ⚙️ 모델 성능
      RMSE / 예측값 분포 / 실제 vs 예측 산점도
```

---

## 4. 기술 스택

| 분류 | 선택 | 이유 |
|------|------|------|
| 빌드 도구 | **Vite** | 빠른 HMR, 현재 표준 |
| 프레임워크 | **React 18** | 컴포넌트 재사용, 상태관리 용이 |
| 언어 | **JavaScript (JSX)** | TypeScript보다 진입장벽 낮음 |
| UI 라이브러리 | **Ant Design** | 테이블/필터/레이아웃 컴포넌트 풍부 |
| 차트 | **ECharts (echarts-for-react)** | 히트맵, 산점도, 라인차트 모두 지원 |
| 스타일 | **CSS Modules + CSS 변수** | 전역 테마 관리 용이 |
| 데이터 파싱 | **PapaParse** | CSV → JSON 변환 |
| 상태 관리 | **useState / useContext** | 초기에는 충분, 복잡해지면 Zustand |
| 배포 | **Vercel** | GitHub 연동, 무료, 1클릭 배포 |

---

## 5. 데이터 구조

### 사용할 파일 3종
```
0_data/
├── compet_xs_data.csv       → 원본 X (die level, 174,980행)
├── y_pred.csv               → 예측값 (unit level, 43,745행)
└── dashboard_agg.csv        → 집계 테이블 (별도 생성 필요)
```

### 집계 테이블 설계 (`dashboard_agg.csv`)
대시보드에서 매번 CSV 전체를 계산하면 느리므로, 미리 집계한 테이블을 만들어 사용한다.

| 컬럼명 | 설명 | 출처 |
|--------|------|------|
| `ufs_serial` | unit 식별자 | xs |
| `run_id` | lot 번호 | xs (run_wf_xy 파싱) |
| `wafer_no` | wafer 번호 | xs (run_wf_xy 파싱) |
| `die_x_mean` | unit 내 die X 좌표 평균 | xs 집계 |
| `die_y_mean` | unit 내 die Y 좌표 평균 | xs 집계 |
| `position` | die position (1~4) | xs |
| `split` | train / val / test | xs |
| `y_pred` | 예측 health 값 | y_pred |
| `is_defect` | y_pred > threshold → 1 | 계산 |
| `risk_level` | high / medium / low | 계산 |
| `shap_top1_feature` | 해당 unit에서 SHAP 1위 feature | shap 값 |
| `shap_top1_value` | SHAP 1위 값 | shap 값 |

### SHAP 집계 테이블 (`shap_summary.csv`)
| 컬럼명 | 설명 |
|--------|------|
| `feature` | feature 이름 (X0 ~ X1086) |
| `mean_shap_defect` | 불량 그룹 평균 SHAP 값 |
| `mean_shap_normal` | 정상 그룹 평균 SHAP 값 |
| `mean_feature_defect` | 불량 그룹 feature 평균값 |
| `mean_feature_normal` | 정상 그룹 feature 평균값 |
| `importance_rank` | 중요도 순위 |

---

## 6. 컴포넌트 구조

```
src/
├── main.jsx                  # 진입점
├── App.jsx                   # 라우팅
├── styles/
│   └── variables.css         # 컬러 변수, 폰트
├── components/
│   ├── layout/
│   │   ├── TopBar.jsx        # 상단 바
│   │   ├── Sidebar.jsx       # 좌측 네비게이션
│   │   └── Layout.jsx        # 전체 레이아웃 래퍼
│   ├── common/
│   │   ├── KpiCard.jsx       # KPI 숫자 카드
│   │   ├── ChartCard.jsx     # 차트 감싸는 카드 컨테이너
│   │   ├── RiskBadge.jsx     # HIGH / MED / LOW 뱃지
│   │   └── ModeToggle.jsx    # Train / Val / Test 전환
│   └── charts/
│       ├── HeatmapChart.jsx  # 웨이퍼맵 히트맵
│       ├── TrendChart.jsx    # 불량률 트렌드 라인
│       ├── BarChart.jsx      # 위치별 수평 바차트
│       ├── ShapChart.jsx     # SHAP 영향도 바차트
│       ├── DistChart.jsx     # 불량/정상 분포 비교
│       └── ScatterChart.jsx  # 실제 vs 예측 산점도
├── pages/
│   ├── Overview.jsx          # 홈 (KPI + 트렌드 + Top-N 테이블)
│   ├── WaferMap.jsx          # 웨이퍼맵
│   ├── LocationAnalysis.jsx  # 위치 분석
│   ├── FeatureAnalysis.jsx   # SHAP + 임포턴스
│   └── ModelPerformance.jsx  # 모델 성능
├── hooks/
│   └── useCSV.js             # CSV 로드 + 파싱 커스텀 훅
└── data/                     # public/ 대신 여기에 CSV 배치 (작은 파일)
    ├── dashboard_agg.csv
    └── shap_summary.csv
```

---

## 7. 개발 단계

### Phase 0 — 환경 세팅 (1일)
- [ ] Node.js LTS 설치
- [ ] `npm create vite@latest dashboard -- --template react`
- [ ] 패키지 설치: `antd`, `echarts`, `echarts-for-react`, `papaparse`
- [ ] CSS 변수 파일 생성 (컬러 팔레트 정의)
- [ ] 기본 레이아웃 (TopBar + Sidebar + 메인 영역) 골격 구성

### Phase 1 — 레이아웃 + 더미 데이터 (3일)
- [ ] TopBar 컴포넌트 (로고, Train/Val/Test 토글, 날짜, 알림)
- [ ] Sidebar 컴포넌트 (메뉴 트리, 활성 상태, 호버 효과)
- [ ] KpiCard 컴포넌트 (숫자, 라벨, 등락 표시)
- [ ] Overview 페이지 — 더미 데이터로 구성
- [ ] 페이지 라우팅 연결

### Phase 2 — 차트 구현 (5일)
- [ ] 웨이퍼맵 히트맵 (ECharts heatmap, die_x/die_y 축)
- [ ] 트렌드 라인차트 (Train 영역차트 + Val/Test 라인)
- [ ] SHAP 수평 바차트 (불량/정상 비교 + SHAP 영향도)
- [ ] Feature Importance 바차트
- [ ] 불량/정상 분포 비교 차트 (밀도 곡선)
- [ ] 위치별(Position / Lot / Wafer) 바차트
- [ ] 모델 성능 — 실제 vs 예측 산점도

### Phase 3 — 실제 데이터 연결 (3일)
- [ ] `dashboard_agg.csv` 생성 (Python에서 집계 후 export)
- [ ] `shap_summary.csv` 생성
- [ ] `useCSV.js` 훅으로 CSV 로드 + 파싱
- [ ] 더미 데이터 → 실제 데이터로 교체
- [ ] Train / Val / Test 모드 전환 시 필터링 연동

### Phase 4 — 인터랙션 + 마무리 (2일)
- [ ] 필터 (Lot / Wafer / Position 드롭다운)
- [ ] 차트 hover 툴팁 상세 정보
- [ ] 상위 N개 불량 unit 테이블 (정렬, 클릭 시 상세)
- [ ] 알림 패널 (위험 unit 목록)
- [ ] 반응형 레이아웃 점검
- [ ] Vercel 배포

---

## 8. 일정

| 기간 | 작업 | 목표 산출물 |
|------|------|------------|
| 04/23 ~ 04/24 | Phase 0: 환경 세팅 + 레이아웃 골격 | 빈 레이아웃 화면 |
| 04/25 ~ 04/27 | Phase 1: 더미 데이터 Overview 완성 | KPI + 트렌드 화면 |
| **04/28** | **중간 발표** | Overview + 웨이퍼맵 시연 가능 수준 |
| 04/29 ~ 05/05 | Phase 2: 전체 차트 구현 | 5개 페이지 더미 완성 |
| 05/06 ~ 05/10 | Phase 3: 실제 데이터 연결 | 실데이터 대시보드 |
| 05/11 ~ 05/15 | Phase 4: 인터랙션 + 배포 | **대시보드 완료** |
| 05/16 ~ 06/08 | 버퍼 + AI Agent 작업 | 추가 기능 |
| **06/09** | **최종 발표** | 전체 시연 |

---

## 9. 중간 발표 (04/28) 준비 체크리스트

중간 발표까지 5일 — 아래 항목만 완성하면 충분히 시연 가능.

- [ ] 전체 레이아웃 (TopBar + Sidebar) 완성
- [ ] Overview 페이지: KPI 카드 3개 + 트렌드 차트
- [ ] 웨이퍼맵: 더미 데이터라도 히트맵 시각화
- [ ] Train / Val / Test 모드 전환 버튼 동작
- [ ] 로컬 실행 가능 상태 (`npm run dev`)

---

## 10. 집계 테이블 생성 (Python 작업)

대시보드 전용 CSV를 미리 만들어두면 React에서 계산 없이 바로 사용 가능.  
아래 Python 코드를 `5_대시보드/make_dashboard_data.py`에 작성 예정.

```python
# 수도 코드 — 실제 구현 시 작성
# 1. xs + y_pred merge (ufs_serial 기준)
# 2. run_wf_xy 파싱 → run_id, wafer_no, die_x, die_y 추출
# 3. unit별 집계: die_x_mean, die_y_mean
# 4. is_defect, risk_level 컬럼 생성 (threshold = 0.015 기준)
# 5. dashboard_agg.csv 저장
#
# 6. SHAP 값 로드
# 7. feature별 불량/정상 그룹 평균 SHAP, feature값 계산
# 8. shap_summary.csv 저장
```

---

## 11. 참고 사항

- **현업 시나리오 반영**: 대시보드 입력은 WT 측정값(X0~X1086) + position + die 좌표만 사용. health 기반 피처(lot/wafer target encoding 등) 미사용
- **데이터 누수 없음**: 대시보드는 예측 결과(`y_pred`)를 표시하는 것이므로 train/val/test 모두 사용 가능
- **threshold**: `y_pred > 0.015`를 불량 기준으로 사용 (CLAUDE.md 기준값)
- **feature 이름**: X0~X1086으로 비식별화되어 있으므로 SHAP 상위 feature는 코드명 그대로 표시
- **AI Agent**: 자연어 질의 기반 의사결정 지원 — "이 lot에서 왜 불량이 많이 나왔나?", "어떤 공정을 개선해야 하나?" 등 PI 관점 질의 응답
