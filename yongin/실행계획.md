# SK Hynix 위험 Unit 사전 탐지 시스템 — 대시보드 & AI Agent 실행 계획

**주제**: Wafer Test 기반 위험 Unit 사전 탐지 시스템
**부제**: 품질 예측 대시보드 구축 및 AI를 활용한 의사결정 지원

> 작성일: 2026-04-23 | 갱신: 2026-05-04
> 최종 발표: 2026-06-09

---

## 0. 프로젝트 목적 & 사용자

### 배경
반도체 생산에서 Field 불량은 **WT 완료 후 수개월이 지나야** 확인된다.

```
WT 완료 → PKG(15일) → PKT(15일) → 현장 투입 → health 수집(1~3개월 소요)
```

이 시간 동안 불량 위험 unit은 이미 출하된 상태이며, 문제를 인지했을 때는
손쓸 수 없는 상황이 된다. **WT 측정값(X0~X1086)으로 미리 위험을 탐지**하는 것이 이 시스템의 존재 이유다.

---

### 목적

> **WT 단계에서 Field 불량 위험이 높은 unit을 사전에 식별하고,
> PI가 즉시 의사결정할 수 있도록 시각화 + AI 분석을 제공한다.**

구체적으로 세 가지를 답한다:

| 질문 | 답 |
|------|-----|
| **지금 위험한 unit이 뭔가?** | Val/Test 예측 결과 기반 위험 unit 목록 |
| **왜 위험한가?** | 주요 불량 유발 Feature 분석 |
| **어디서 났는가?** | Lot / Wafer / Position / 웨이퍼맵 |

---

### 데이터 시나리오 (핵심)

이 시스템은 **두 가지 데이터 상태**를 동시에 다룬다.

```
Train  →  WT 값 + 실제 health(Y) 모두 있음
           "과거 생산분 — 불량 패턴이 이미 확인된 데이터"
           역할: 모델 학습 + 패턴 파악

Val    →  WT 값만 있음, health(Y)는 아직 없음
           "방금 생산된 제품 — 아직 Field 결과가 안 나온 상태"
           역할: 실제 예측 대상 (PI가 지금 봐야 하는 것)

Test   →  Val과 동일한 상태 (최종 제출용)
```

> **대시보드의 메인은 Val이다.**
> "지금 이 unit들이 위험한가?"에 답하는 것이 핵심.
> Train은 "왜 이 패턴이 위험한지" 설명하는 보조 역할.

---

### 표현 원칙

모델 성능 지표(RMSE, AUC, clf_proba 수치)는 전면에 노출하지 않는다.
PI가 알아야 하는 것은 **"이 unit이 위험한가, 얼마나, 왜"**이지 모델 평가 지표가 아니다.

| 내부 표현 | 화면 표현 |
|----------|----------|
| reg_pred ≥ HIGH threshold (최신 lot 상위 10%) | 🔴 고위험 |
| reg_pred ≥ 불량 threshold (HIGH 미만) | 🟡 주의 |
| reg_pred < 불량 threshold | 🟢 정상 |
| reg_pred 수치 | 예측 불량지수 |
| health > 0 (Train) | 실제 불량 확인 |
| RMSE, AUC 등 | 모델 성능 탭에서만 (별도 분리) |

> **위험 기준 근거**: clf_proba 분류기 Recall 평균 0.011로 랜덤 수준. 위험 분류 기준은 reg_pred(회귀 예측값)로 정의.
> - **불량 threshold**: train reg_pred 상위 29.2% 경계값 (train 실제 불량 비율 29.2%와 동일 기준으로 전체 고정). 대시보드 로드 시 동적 계산.
> - **HIGH threshold**: 최신 lot(어제 하루치) unit 안에서 reg_pred 상위 10% 분위수 (KPI 전용, 하루치 기준 동적 계산).
> - **트렌드 그래프**: train은 `health > 0` 기준 실제 수율, val은 `reg_pred >= 불량 threshold` 기준 예측 수율. 같은 척도(수율%)로 비교하여 모델 신뢰도 확인. x축은 날짜(YYYY-MM-DD).

---

### 사용자
- **1차**: PI (Process Integration 엔지니어) — 수율 점검, 불량 원인 파악, 공정 개선
- **2차**: 포트폴리오 열람자 — "현업 수준의 ML 시스템을 설계·구현했다"는 것을 보여주는 결과물

### PI 의사결정 흐름
```
① 위험 현황 파악   →  Overview (지금 위험한 unit 몇 개?)
② 위치 파악        →  웨이퍼맵 / 위치 분석 (어느 Lot, 어느 위치?)
③ 원인 파악        →  변수 분석 (왜 위험한가?)
④ 조치 결정        →  AI Agent (어떻게 해야 하나?)
⑤ 보고서 작성      →  AI Agent (보고서 자동 생성)
```

---

## 1. 대시보드 vs Agent 역할 분리

### 핵심 원칙
```
대시보드 = "무슨 일이 일어나고 있나"  →  숫자와 차트로 현상 시각화
Agent    = "그래서 어떻게 해야 하나"  →  원인 분석 + 조치 제안 + 보고서 생성
```

### 대시보드가 답하는 것 (정적 시각화)
| PI 질문 | 대응 화면 |
|---|---|
| 지금 위험한 unit이 몇 개야? | Overview — 불량률 / 위험집중 Lot / HIGH 평균 불량지수 KPI 카드 |
| 어느 lot에서 많이 났어? | 위치 분석 — Lot 바차트 (Lot 클릭 → Wafer 바차트 → 웨이퍼맵 드릴다운) |
| 가장 위험한 unit이 어디야? | 위험 Unit 목록 — Top N 테이블 |
| 왜 위험하다고 판단했어? | 변수 분석 — Feature Importance |
| 불량/정상 그룹 차이가 뭐야? | 그룹 비교 |
| 특정 unit 상세가 궁금해 | 데이터 테이블 |

### Agent가 답하는 것 (동적 추론)
| PI 질문 | Agent 기능 |
|---|---|
| 왜 이번 lot A에서 특히 많이 났어? | 원인 분석 |
| 이 패턴 이전에도 있었어? | 패턴 비교 |
| X005가 높으면 어떻게 조치해야 해? | 조치 제안 |
| 개선조치 보고서 써줘 | 보고서 자동 생성 |
| 앞으로 어떻게 막아? | 방어선 제안 |

---

## 2. 전체 레이아웃

```
┌─────────────────────────────────────────────────────────┐
│  TopBar: 로고 / 날짜 / 알림 🔔 / 프로필                  │
├─────────────┬───────────────────────────────────────────┤
│             │                                           │
│  좌측       │  메인 콘텐츠 영역                          │
│  사이드바   │  (선택 메뉴에 따라 변경)                   │
│             │                                           │
│  📊 Overview│                                           │
│  🗺 웨이퍼맵│                                           │
│  📍 위치분석│                          [🤖 챗봇 버튼]   │
│  🔬 변수분석│                                           │
│  📈 그룹비교│                                           │
│  📋 데이터  │                                           │
└─────────────┴───────────────────────────────────────────┘
```

---

## 3. 메인페이지 (Overview) 구성

**"지금 얼마나 위험한가"를 한눈에 파악하는 페이지 — 최신 lot(어제 하루치) 기준**

```
┌─────────────────────────────────────────────────────────┐
│  📅 기준일: YYYY-MM-DD (Lot N) — 어제 WT 완료분           │
│  KPI 카드 3개  (어제 WT 완료 lot 하루치 기준)              │
│  [📊 불량률 N%      ]  [🏭 위험집중 Wafer]  [🔴 HIGH 평균 불량지수]  │
│  [위험 N개 / 전체 N개]  [Wafer XX      ]  [reg_pred 평균값      ]  │
├─────────────────────────────────────────────────────────┤
│  수율 트렌드 (날짜 x축, train+val만, test 제외)            │
│  ━━━ 예측 수율%(라인, val)  ▓▓▓ 실제 수율%(영역, train)   │
│  |←── 03-27~05-11(train) ──→|←── 05-12~06-10(val) ──→| │
│                              ↑ 구분선   가장 오른쪽=어제  │
│  [드래그 슬라이더로 날짜 범위 조절 가능]                   │
├──────────────────────────┬──────────────────────────────┤
│  Wafer별 위험 unit 수     │  위험 등급별 unit 수          │
│  (어제 하루치 기준)        │  (어제 하루치 기준)           │
└──────────────────────────┴──────────────────────────────┘
```

### 메인페이지에 넣는 것
- KPI 카드 3개 (**최신 lot — 어제 하루치 기준**, 기준일 텍스트 상단 표시):
  - **불량 위험률** — 최신 lot unit 중 `reg_pred >= 불량 threshold` 비율(%). 아래 작게 "위험 N개 / 전체 N개"
  - **위험 집중 Wafer** — 최신 lot 안에서 위험 unit이 가장 많은 wafer 번호
  - **HIGH 그룹 평균 불량지수** — 어제 lot 안에서 y_pred 상위 10% unit들의 평균값
- **수율 트렌드 차트**: 날짜(YYYY-MM-DD) x축, train+val만(test 제외), 가장 오른쪽=어제. 예측 수율(라인) + 실제 수율(영역, train만). 하단 드래그 슬라이더로 날짜 범위 조절 가능.
- **Wafer별 위험 unit 수 바차트** — 어제 하루치 기준
- **위험 등급별 unit 수 바차트** — 어제 하루치 기준 (HIGH/MED/LOW)

### 메인페이지에 넣지 않는 것
- Lot별 위험 집중도 바차트 → 웨이퍼맵 페이지로 이동
- Position별 위험도 바차트 → 위치 분석 페이지로 이동
- Train/Val/Test 탭 — 트렌드 차트 안에서 구분선으로 처리
- 위험 Unit Top 10 테이블 — 모델 신뢰도 한계로 제거
- RMSE, AUC, clf_proba 수치 → 모델 성능 탭으로만
- 피처 분석, 그룹 비교 → 사이드바 세부 페이지

---

## 4. 사이드바 페이지 구성

현재 구현 상태와 데이터 연결 여부를 함께 표기한다.

| 메뉴 | 페이지 | 데이터 | 상태 |
|------|--------|--------|------|
| 📊 Overview | 위험 현황 요약 | dashboard_units.csv | ✅ 구현 완료 |
| 🗺 웨이퍼맵 | Lot→Wafer→die 히트맵 드릴다운 | wafer_map.csv | ✅ 구현 완료 |
| 📍 위치 분석 > Position | position별 위험도 | dashboard_units.csv | ✅ 구현 |
| 📍 위치 분석 > Lot | lot별 위험 unit 수 | wafer_map.csv (run_id) | 🟡 데이터 있음, 미구현 |
| 📍 위치 분석 > Wafer | wafer별 위험도 | wafer_map.csv (wafer_no) | 🟡 데이터 있음, 미구현 |
| 🔬 변수 분석 > SHAP | feature 영향도 | feature_importance.csv | 🟡 SHAP 아님, Importance로 대체 표시 중 |
| 🔬 변수 분석 > 중요도 | Feature Importance | feature_importance.csv | ✅ 구현 |
| 🔬 변수 분석 > 분포 | 변수 분포 비교 | (원본 feature CSV 필요) | 🔴 더미 — 원본 없음 |
| 📈 그룹 비교 | 불량/정상 그룹 차이 | feature_importance.csv + group mean | 🔴 더미 — 구현 필요 |
| 📋 데이터 테이블 | unit 상세 목록 | dashboard_units.csv | ✅ 구현 |
| 📈 모델 성능 | RMSE/AUC 상세 | metrics.csv, oof_meta.csv | ✅ 구현 (사이드바에서 숨김 고려) |

> **🚨 위험 Unit 목록 페이지** 추가 예정 — Val 기준 HIGH 위험 unit 목록 (Top N), Lot/Wafer 정보 포함, PI가 즉시 조치할 수 있는 실행 리스트

---

### 📊 Overview (메인)
**"지금 얼마나 위험한가"** — 구현 완료 (2026-06-11)
- KPI 카드 3개 (**최신 lot — 어제 하루치 기준**, 기준일 상단 표시):
  - **불량 위험률** — 최신 lot unit 중 `reg_pred >= 불량 threshold` 비율(%)
  - **위험 집중 Wafer** — 최신 lot 안에서 위험 unit이 가장 많은 wafer 번호
  - **HIGH 그룹 평균 불량지수** — 어제 lot 안에서 y_pred 상위 10% unit들의 평균값
- **수율 트렌드 차트**: 날짜 x축, train+val만(test 제외), 가장 오른쪽=어제. 드래그 슬라이더로 범위 조절 가능
- **Wafer별 위험 unit 수 바차트** — 어제 하루치
- **위험 등급별 unit 수 바차트** — 어제 하루치 (HIGH/MED/LOW)

---

### 🚨 위험 Unit 목록 (신규 추가)
**"지금 당장 확인해야 하는 unit이 뭔가"**
- 기준: Val/Test 중 HIGH 위험 unit 전체
- 컬럼: Unit ID / Lot(run_id) / Wafer / Position / 위험등급 / 예측 불량지수
- 정렬: 예측 불량지수(reg_pred) 높은 순
- 데이터: dashboard_units.csv + wafer_map.csv 조인

---

### 🗺 웨이퍼맵
**"어느 위치에서 불량이 났는가"** — 구현 완료 (2026-06-11)
- **Step 1**: Lot별 위험 unit 수 막대그래프 (val 전체 기간, 날짜 x축) — 막대 클릭 시 Step 2
- **Step 2**: 선택 Lot의 Wafer별 위험 unit 수 막대그래프 — 막대 클릭 시 Step 3
- **Step 3**: 선택 Wafer의 웨이퍼맵 (die_x/die_y scatter, 색상=예측 불량지수, 초록→노랑→빨강)
- 데이터: wafer_map.csv (die 단위, pred 컬럼 사용)

---

### 📍 위치 분석
**"어떤 lot/wafer/position이 문제인가"**

- **Position 탭**: position 1~4별 위험 unit 비율 (구현됨, dashboard_units.csv)
- **Lot 탭**: run_id별 위험 unit 수 바차트 → 🟡 wafer_map.csv 사용, 미구현
- **Wafer 탭**: wafer_no별 위험도 → 🟡 wafer_map.csv 사용, 미구현

---

### 🔬 변수 분석
**"왜 불량으로 판단했는가"**

- **Feature Importance 탭**: LGBM/ET/ElasticNet gain 기준 Top 30 바차트 (구현됨)
- **SHAP 탭**: 현재 feature_importance.csv로 대체 표시 중 (🟡 SHAP 원본 없음 명시)
- **분포 탭**: 원본 feature CSV 미존재 → 🔴 더미 유지

---

### 📈 그룹 비교
**"불량/정상 그룹이 얼마나 다른가"**
- 상위 N개 feature의 불량/정상 그룹 평균값 비교
- "X1083이 불량 그룹에서 2.3배 높다" 형태
- 데이터: feature_importance.csv + dashboard_units.csv에서 group mean 계산
- 🔴 더미 상태 — 구현 필요

---

### 📋 데이터 테이블
**"특정 unit 상세 확인"**
- 전체 unit 목록 (train/val/test 탭)
- risk level 필터, Unit ID 검색
- clf_proba(위험도) / reg_pred(예측 불량지수) / health(실제, train만 공개) 표시
- 🟡 val/test health는 "미공개" 표시 (의도적)

---

## 5. 챗봇 (AI Agent) 구성

### 열기 방식
- 우측 하단 고정 버튼 (🤖)
- 클릭 시 오른쪽에서 슬라이드로 패널 열림
- 메인 콘텐츠 영역이 좁아지는 push 방식
- 닫으면 원래대로 복귀

### 탭 구조
```
┌─────────────────────────────┐
│  [  Assistant  ] [  Agent  ] │  ← 탭 클릭으로 전환
├─────────────────────────────┤
│                             │
│  Assistant 탭:              │
│  (더미 — 추후 구현)          │
│                             │
│  Agent 탭:                  │
│  (더미 — 추후 구현)          │
│                             │
├─────────────────────────────┤
│  [입력창          ] [전송]   │
└─────────────────────────────┘
```

### Assistant 탭
- 단순 질문 답변 (더미)
- 예: "불량률이 뭐야?", "SHAP이 뭐야?"

### Agent 탭
- 데이터 기반 분석 + 추론 + 보고서 생성 (더미)
- 예: "이번 lot 왜 불량 많아?", "보고서 써줘"

---

## 6. 컴포넌트 구조

```
src/
├── main.jsx
├── App.jsx                        # 라우팅
├── styles/
│   └── variables.css              # 컬러 변수, 폰트
├── components/
│   ├── layout/
│   │   ├── TopBar.jsx             # 상단 바 (로고, 날짜, 알림, 프로필)
│   │   ├── Sidebar.jsx            # 좌측 네비게이션
│   │   └── Layout.jsx             # 전체 레이아웃 래퍼
│   ├── common/
│   │   ├── KpiCard.jsx            # KPI 숫자 카드
│   │   ├── ChartCard.jsx          # 차트 컨테이너 카드
│   │   └── RiskBadge.jsx          # HIGH / MED / LOW 뱃지
│   ├── charts/
│   │   ├── HeatmapChart.jsx       # 웨이퍼맵 히트맵
│   │   ├── MiniHeatmap.jsx        # Overview용 미니 웨이퍼맵
│   │   ├── TrendChart.jsx         # 불량률 트렌드
│   │   ├── BarChart.jsx           # 위치별 바차트
│   │   ├── ShapChart.jsx          # SHAP 영향도
│   │   ├── DistChart.jsx          # 불량/정상 분포 비교
│   │   └── GroupCompareChart.jsx  # 그룹 비교 바차트
│   └── chatbot/
│       ├── ChatBot.jsx            # 챗봇 패널 (슬라이드)
│       ├── ChatBotTab.jsx         # Assistant / Agent 탭 전환
│       └── ChatBot.css
├── pages/
│   ├── Overview.jsx               # 메인 (KPI + 트렌드 + 미니맵 + Top N)
│   ├── WaferMap.jsx               # 웨이퍼맵 상세
│   ├── LocationAnalysis.jsx       # 위치 분석 (lot → wafer → position)
│   ├── FeatureAnalysis.jsx        # 변수 분석 (SHAP + 분포)
│   ├── GroupCompare.jsx           # 그룹 비교
│   └── DataTable.jsx              # 데이터 테이블
└── hooks/
    └── useCSV.js                  # CSV 로드 + 파싱
```

---

## 7. 디자인 방향

### 컬러 팔레트
| 역할 | 색상 | 코드 |
|------|------|------|
| Primary (사이드바, 강조) | 남색 | `#1E3A5F` |
| Secondary (버튼, 포인트) | 밝은 파랑 | `#3B82F6` |
| Accent (호버, 하이라이트) | 하늘색 | `#60A5FA` |
| 배경 | 흰색/연회색 | `#F8FAFC` |
| 카드 배경 | 흰색 | `#FFFFFF` |
| 위험 (불량) | 빨강 | `#EF4444` |
| 경고 | 주황 | `#F97316` |
| 정상 | 초록 | `#22C55E` |
| 텍스트 기본 | 짙은 회색 | `#1E293B` |
| 텍스트 보조 | 회색 | `#64748B` |

### 디자인 원칙
- 라이트 테마 (흰색 배경, 남색 사이드바)
- 카드 기반 레이아웃 (그림자 + 라운드 모서리)
- 한 화면에 필요한 정보만 깔끔하게
- 호버 시 카드 테두리 강조 + 그림자 증가

---

## 8. 기술 스택

| 분류 | 선택 | 이유 |
|------|------|------|
| 빌드 도구 | Vite | 빠른 HMR |
| 프레임워크 | React 18 | 컴포넌트 재사용 |
| 언어 | JavaScript (JSX) | 진입장벽 낮음 |
| UI 라이브러리 | Ant Design | 테이블/필터/레이아웃 풍부 |
| 차트 | ECharts (echarts-for-react) | 히트맵, 바차트, 라인 모두 지원 |
| 스타일 | CSS Modules + CSS 변수 | 전역 테마 관리 |
| 데이터 파싱 | PapaParse | CSV → JSON |
| 상태 관리 | useState / useContext | 초기 충분, 복잡해지면 Zustand |
| 배포 | Vercel | GitHub 연동, 무료 |

---

## 9. 데이터 구조

### 실제 사용 파일 (public/)

```
public/
├── dashboard_units.csv      → unit-level 예측 결과 (메인 데이터)
├── wafer_map.csv            → die-level 실좌표 + 예측 (웨이퍼맵용)
├── feature_importance.csv   → feature별 중요도 (변수 분석용)
├── metrics.csv              → 모델 성능 지표 (모델 성능 탭용)
└── oof_meta.csv             → OOF 예측 원본 (scatter 등)
```

> ❌ `shap_summary.csv` — 미존재. SHAP 계산 미수행. Feature Importance로 대체.
> ✅ `dashboard_dates.csv` — lot/wafer/날짜 재매핑 후 재생성 완료 (2026-05-04)

---

### 대시보드용 날짜·Lot·Wafer 재매핑 (2026-05-04)

**배경**: 원본 데이터는 5일치를 수집 후 같은 lot/wafer 안에서 6:2:2 random split.
train/val이 시간 차이 없이 동일 생산 배치에 섞여 있어 시계열 서사가 불가능했음.

**해결**: 모델 예측값(clf_proba, reg_pred)은 그대로 유지하고, 대시보드 표시용 lot/wafer/날짜만 재매핑.

```
오늘: 2026-06-11 (대시보드 기준일)
어제: 2026-06-10 = 최신 val lot 56 = KPI 기준일

train: lot  1~28  / 2026-03-27 ~ 2026-05-11 (46일 구간)
val:   lot 29~56  / 2026-05-12 ~ 2026-06-10 (하루 1 lot, 30일치)
test:  lot 57~84  / 2026-06-11 이후 (대시보드 미표시)

비율: train 60% / val 20% / test 20% = 6:2:2 유지
서사: train = 과거 생산 완료분 (Y값 확인됨)
      val   = 최근 생산분, 어제까지 (아직 Y값 없음, 예측 대상)
      test  = 오늘 이후 (미래, 대시보드 트렌드에서 제외)
```

> **주의**: lot/wafer/날짜는 대시보드 시각화용으로 재구성된 값. 모델 학습에 사용된 원본 run_id와 다름.

---

### dashboard_units.csv (43,745 rows — unit 단위)
| 컬럼 | 설명 | 비고 |
|---|---|---|
| `ufs_serial` | unit 식별자 | S00000~ |
| `run_id` | Lot 번호 (재매핑) | train 1~28 / val 29~56 / test 57~84 |
| `wafer_no` | Wafer 번호 (재매핑) | split 내 1~25 재번호 |
| `date` | 생산 날짜 (재매핑) | lot 단위로 날짜 부여 |
| `split` | train / val / test | |
| `health` | 실제 불량 값 | train만 의미 있음, val/test = 미공개 |
| `clf_proba` | 불량 확률 (Stage 1 분류기) | 화면 표현: 위험도 |
| `reg_pred` | 예측 불량지수 (Stage 2 회귀) | 화면 표현: 예측 불량지수 |
| `risk` | HIGH / MED / LOW | reg_pred 기준 (상위 10% → HIGH / 불량 threshold 이상 → MED / 미만 → LOW) |

---

### wafer_map.csv (174,572 rows — die 단위)
| 컬럼 | 설명 | 비고 |
|---|---|---|
| `ufs_serial` | unit 식별자 | |
| `run_id` | Lot 번호 | run_wf_xy 파싱 |
| `wafer_no` | Wafer 번호 | run_wf_xy 파싱 |
| `die_x` | die X 좌표 (실좌표) | run_wf_xy 파싱 |
| `die_y` | die Y 좌표 (실좌표) | run_wf_xy 파싱 |
| `position` | die의 unit 내 위치 (1~4) | |
| `split` | train / val / test | |
| `health` | 실제 불량 값 | train만 의미 있음 |
| `clf_proba` | 불량 확률 | |
| `reg_pred` | 예측 불량지수 | |
| `risk` | HIGH / MED / LOW | |

---

### feature_importance.csv (feature 단위)
| 컬럼 | 설명 |
|---|---|
| `feature` | feature명 (X0~X1086) |
| `lgbm_gain` | LGBM gain 기준 importance |
| `et_gain` | ExtraTrees impurity 기준 importance |
| `enet_coef` | ElasticNet 계수 절대값 |
| `lgbm_rank` | LGBM importance 순위 |
| `et_rank` | ET importance 순위 |
| `enet_rank` | ElasticNet 순위 |

---

### metrics.csv (모델 성능 지표)
| 컬럼 | 설명 |
|---|---|
| `stage` | reg (회귀) / clf (분류) |
| `model` | lgbm / et / enet |
| `split` | val / test / train |
| `metric` | rmse / auc / recall / ap 등 |
| `value` | 수치 |

---

## 10. 참고 사항

- **현업 원칙**: 입력은 WT 측정값(X0~X1086) + position + die 좌표만 사용. health 기반 피처 미사용
- **threshold**: train reg_pred 상위 29.2% 경계값 (대시보드 로드 시 동적 계산). health 실제값 없이 새 배치에도 적용 가능한 현업 기준.
- **feature 이름**: X0~X1086 비식별화 → SHAP 상위 feature는 코드명 그대로 표시
- **Agent**: 더미로 구현 후 추후 Claude API 연동
